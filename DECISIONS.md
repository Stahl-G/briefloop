# BriefLoop 产品行为决策记录

**用途**:记录**单个功能**的行为约定——语义、边界条件、口径。
**不写在这里的**:跨功能的视觉与交互规则 → `DESIGN.md`。

**判定标准**:换一个功能仍然成立 → `DESIGN.md`;只对某个功能成立 → 本文件。

**格式**:每条决策写明「结论」+「理由/边界」。新增条目追加到对应功能小节,并在文末登记日期。

> 本文件的初始内容从 `DESIGN.md` v1 的 §4.4、§6、「搜索策略设置」「报告快捷选项」「稿件改动」拆出。原始条目未改写语义,日期标记为「≤2026-09-18(v1 拆分,原始日期不详)」。

---

## 1. 文案口径

### 1.1 受控工具额度

**结论**:任何「受控工具额度」相关文案**不得**表述为宿主费用上限。

**理由**:额度是内部执行约束,不是用户侧的计费承诺;二者混淆会构成对用户的费用误导。

**参考**:plan v0.2 §7
**日期**:≤2026-09-18(v1 §4.4 拆出)

---

## 2. 运行时 / 宿主

### 2.1 模型归属

**结论**:当前选择的模型来自**当前工作区设置**。

**边界**:
- 其他宿主**不沿用**当前宿主的模型
- **不**将模型目录中的首项当作已配置模型

**理由**:两种默认行为都会让用户误以为某宿主已完成配置,实际未配置。

**日期**:≤2026-09-18(v1 §6 拆出)

### 2.2 CLI 检测语义

**结论**:检测到 CLI **不表示**账号调用成功。

**边界**:两者是独立状态,UI 上不得用同一个「已连接」表述覆盖。

**日期**:≤2026-09-18(v1 §6 拆出)

### 2.3 宿主选择器

**结论**:首页只保留**可操作的**宿主选择器;移除重复的「当前」宿主标签。

**边界**:已运行回合所使用的宿主保留在**执行记录**中,不因当前选择变化而改写。

**日期**:≤2026-09-18(v1 §6 拆出)

---

## 3. 定时报告

### 3.1 执行时机

**结论**:定时报告**仅在本工作区服务运行期间**执行。

**边界**:
- 关闭期间的时点**不补跑**
- 该约束必须在 UI 上明示,不能只写在文档里

**理由**:用户会按「定时」的常识预期离线补跑;不明示等于承诺了做不到的事。

**日期**:≤2026-09-18(v1 §8 拆出)

### 3.2 暂停 / 删除的影响范围

**结论**:暂停或删除定时计划**不影响**已生成的报告。

**日期**:≤2026-09-18(v1 §8 拆出)

---

## 4. 报告删除

### 4.1 删除语义

**结论**:删除报告**只把报告移出列表**。以下内容保留:

- 来源
- 已下载文件
- 内部核查与学习的引用记录

**理由**:这些是其他报告与学习机制的共享依赖,硬删会造成引用悬空。

**边界**:UI 必须让用户理解「删除」不等于「清除数据」,否则构成隐私预期错配。

**日期**:≤2026-09-18(v1「稿件改动」拆出)

### 4.2 删除的前置条件

**结论**:运行中任务未结束时,**拒绝**删除。

**日期**:≤2026-09-18(v1「稿件改动」拆出)

### 4.3 删除入口

**结论**:删除放在「更多操作」菜单,需二次确认。

> 此条为交互约定,与 `DESIGN.md` §6.1(顶栏只放对象级动作)配套。保留在此处是因为「需二次确认」是基于 4.1 数据语义的功能决策。

**日期**:≤2026-09-18(v1「稿件改动」拆出)

---

## 5. 稿件改动对比

### 5.1 对比基线

**结论**:默认与**上一稿**对比,可切换到第一稿或任一中间稿。

**日期**:≤2026-09-18(v1「稿件改动」拆出)

### 5.2 标记的作用域

**结论**:改动标记(新增下划线 / 删除删除线 / 格式变化边框)**不写入**:

- 正文
- 保存的版本
- Word 导出

**理由**:标记是视图层产物。写入任一持久化路径都会污染交付物。

**日期**:≤2026-09-18(v1「稿件改动」拆出)

---

## 6. 搜索策略

### 6.1 保存方式

**结论**:搜索策略**显式保存**,不自动保存。

**日期**:≤2026-09-18(v1「搜索策略设置」拆出)

### 6.2 密钥处理

**结论**:密钥独立配置区,保存后**不回显**。

**日期**:≤2026-09-18(v1「搜索策略设置」拆出)

### 6.3 未接通平台

**结论**:未接通的平台**不展示选择入口**。

> 与 `DESIGN.md` §6.3(空态不占位)同源。

**日期**:≤2026-09-18(v1「搜索策略设置」拆出)

### 6.4 用量口径

**结论**:宿主原生用量未知时**单独呈现**,不与已知用量合并展示,不推算。

**日期**:≤2026-09-18(v1「搜索策略设置」拆出)

---

## 7. 报告执行参数

### 7.1 参数分层

**结论**:

| 参数 | 位置 |
|---|---|
| 研究档位、事实核查、WikiSkill 及轮数、执行上限、Scout 并发 | composer 的「参数」popover |
| 独立报告并发 | 设置页 |

**边界**:不为这些参数增加大卡片。

> 承载形态见 `DESIGN.md` §7.2。

**日期**:≤2026-09-18(v1「报告快捷选项」拆出)

### 7.2 用时口径

**结论**:排队等待与实际执行**分开显示**。无执行记录时**不推算**执行用时。

**日期**:≤2026-09-18(v1「报告快捷选项」拆出)

---

## 登记

| 日期 | 变更 |
|---|---|
| 2026-09-18 | 从 `DESIGN.md` v1 拆出,建立本文件。原始条目语义未改,日期不详者标记为「≤2026-09-18」 |
