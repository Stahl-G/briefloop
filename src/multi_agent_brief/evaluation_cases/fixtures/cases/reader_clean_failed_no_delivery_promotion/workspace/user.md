# Synthetic Reader-Clean Failure Fixture

This public-safe workspace fixture proves that a failed reader-clean finalize
run does not create current delivery truth, even when a stale delivery file is
present on disk.
