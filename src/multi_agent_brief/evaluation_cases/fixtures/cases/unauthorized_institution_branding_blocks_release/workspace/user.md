# Synthetic Unauthorized Branding Case

This public-safe fixture checks that institution-branded release readiness
metadata blocks when authorization is explicitly absent. It is not public
release authorization and uses only synthetic names.
