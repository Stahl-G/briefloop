# BriefLoop 0.17.0

一个基于 Codex CLI 的个人本地 Agent 工作台：围绕你的问题寻找来源、生成简报、独立评价，并把反馈整理成后续可用的经验。

本版界面与文档以中文为主。国际化和行业 Deep Research 留到后续版本。

## 快速开始

```sh
git clone https://github.com/Stahl-G/briefloop.git
cd briefloop
./start.sh
```

只需这一个仓库。首次启动自动创建 `.venv`、安装 Python 依赖和随仓库提供的 WikiSkill wheel、启动本地服务并打开网页；无需第二次 clone，也无需手动构建前端。

使用前需要：

- macOS，以及 Python 3.11 或更新版本。
- 已安装并完成认证的 Codex CLI 或 Opencode CLI；对话和任务执行需要其中之一。
- 首次安装依赖需要网络。使用 Tavily 时另行配置自己的 Tavily Key。

也可指定目录、端口和后端：

```sh
./start.sh --workspace /path/to/workspace --port 8765 --no-open --backend opencode
```

## 执行后端：Codex / Opencode

工作区默认使用 Codex（Responses 通道）。新任务可在启动时用 `--backend opencode` 切换，或在网页设置页切换；已排队/已开始的任务冻结原后端，不跟随切换。

- Opencode 模型填完整 `provider/model`（如 `opencode-go/gpt-5.6-luna`），以本机 `opencode` 已登录的可选模型为准；BriefLoop 不枚举、不限制 provider。
- 推理档位在 Opencode 下叫 variant（如 high / max），留空为默认；聊天沿用工作区主模型的 variant。
- 限制（以诚实为准）：Opencode 没有每轮网络硬开关，关闭联网时靠指令与权限执行，bash 仍可能联网；不支持运行中追加提问与交互式提问；图片附件仅视觉模型可读。服务端版本要求 1.x。
- 排查：任务失败先看任务日志；"模型 ID"类错误检查 `provider/model` 拼写与 `opencode auth login`；"90 秒未开始"多为模型不可用或登录失效。

服务只监听本机。关闭网页不会停止正在执行的任务；从网页“停止”或关闭服务进程结束执行。

## 现在可以做什么

- **对话工作台**：发消息、上传材料、选择模型、排队或中途补充要求，查看公开工具和子 Agent 活动。
- **工作区管理**：打开已有目录或新建空工作区，各自保存对话、来源、稿件、评分与 Wiki。
- **对话整理**：归档、移到回收站、恢复，或批量归档已结束对话；报告、来源与 Wiki 不跟随对话清理删除。
- **从公开资料写简报**：不强制上传。开启联网后，由 Scout 查找并筛选资料；Analyst 写稿，Evaluator 独立评分。
- **直接改稿与学习**：编辑正文或留下反馈；Wiki Maintainer 整理经验，Skill Proposer 提出技能，Evaluator 比较实际产物后决定是否采用。
- **明确的预算与篇幅**：整次报告共享检索额度；正文目标字数和上限可直接修改。

## 第一次使用

1. 在“材料与需求”填写目的、读者、时间范围和字数要求。可上传材料，也可仅开启联网。
2. 选择搜索工具。Tavily 的输入口就在联网开关下；保存 Key 不会自动验证或发起收费搜索。
3. 在“设置”确认主模型及角色模型。模型 ID 可自行填写；可选 provider 名称沿用本机 Codex 已配置的 Responses provider，不限定 OpenAI 型号。
4. 点击生成。草稿保存后即可查看，评分独立完成；低分或评分失败不会隐藏原稿。
5. 按需修改正文、保存反馈或明确启动学习。注意自动学习开关：启用后，编辑静默约 30 秒会合并反馈并启动模型任务。

### 三个学习与评价角色

| 角色 | 职责 |
| --- | --- |
| Evaluator | 给产物评分；比较候选产物，判断是否改善 |
| Wiki Maintainer | 将反馈和执行经验整理成 Wiki |
| Skill Proposer | 根据 Wiki 提出或改进 Skill |

Evaluator 的单稿评分与产物比较共用角色配置，每次使用独立模型会话。候选技能已生成但验证未完成时，也能在 Wiki 页面查看；不会因为产生了文件就直接启用。

报告页的「已保存版本检查」分别展示断链引用、数值绑定与导出提示。数值绑定需要原文摘录、正文唯一片段及其中的完整数值；程序按币种、单位和正负号核对，只对成功定位的项目计数。无绑定、单位不支持或改稿使定位失效时显示未检查，不据此宣称可正式交付。数值匹配不证明主体、期间、财务指标或政策条件正确，这些仍由 Evaluator 对照原文评价。草稿仍可保存、修改和下载；含图表的独立交付请使用 Word 或包含图片的 Markdown 包。


## 研究预算

| 预设 | 搜索调用尝试 | 去重候选 URL | 全文来源 URL |
| --- | ---: | ---: | ---: |
| 周报 | 12 | 60 | 18 |
| 月报 | 30 | 200 | 45 |

预算由整次报告中的 Scout 共享，不是每个 Scout 各拿一份，也不要求花完。失败的 Tavily Search 调用仍计次；同一 URL 的直接获取、Extract 回退及重试不重复扣全文 URL。达到额度后保留材料、说明缺口，不自动加额。

搜索请求和候选额度适用于受控 Tavily 工具；原生 Codex 搜索无法在此精确计量，页面会明确区分。全文额度统计受控工具处理的不同 URL，不是 PDF 页数、阅读完成数量或所有 HTTP 请求次数。

## 正文篇幅

| 预设 | 目标字数 | 字数上限 |
| --- | ---: | ---: |
| 简短 | 800 | 1,000 |
| 标准 | 1,500 | 2,000 |
| 详细 | 2,000 | 2,500 |

数字可直接修改。中文汉字每字计 1，连续英文或数字串计 1；格式、URL 和来源引用标记不计。原文摘录与定位放在来源记录中。页面显示保存后的实际计数，旧稿没有设置过数字要求时如实显示“未设置”。

## 行业定期报告（开发中新增）

“材料与需求”可选择“行业定期报告”，填写行业、目标组织、报告日期和覆盖期间。默认目标 5,000、上限 5,500，可自由修改；六类章节依据行业和任务裁剪。公司、行业和历史稿保存在用户工作区，公共代码不包含特定客户数据。

- 已上传历史报告可以单独指定为结构与风格参考，不作为本期事实来源；不上传也能在开启联网后开始研究。
- 研究角色整理本期指标、前值、日期、单位和来源，调用确定计算工具；关键缺口可在剩余共享预算内定向补查，拿不到的数据列出缺口和影响。
- “数据与缺口”按钮可查看保存的原始值、变化和来源，不调用模型。没有记录缺口不代表资料完整。
- Word 下载使用通用 A4 蓝色报告样式，保留表头、页眉日期、组织和页码、真实网页超链接；有同口径、同预测截至日的数据时附一张预测图。此版本提供内置版式，尚不支持任意上传 DOCX 模板的逐项复刻。
- 手动改稿不会自动改写结构化数据；数字变化后保留原数据供核对，Word 暂不附可能过时的原数据图。

详细使用与数据接口见 [行业报告](docs/行业报告.md)。本次验证采用合成资料，未据此宣称真实行业覆盖率、事实正确率或成本改善。

## 升级、边界与文档

0.17.0 是架构升级。旧提交和发布标签保留；旧版工作区不自动迁移。请保留旧目录，在新工作区按需要导入原始材料。[升级说明](docs/升级说明.md)

检索预算不保证覆盖完整，模型评分也不代表事实正确率。本版没有重新开展效果或 Token 节省实验，不将离线测试描述为质量提升证明。

- [使用指南](docs/使用指南.md)
- [变更记录](CHANGELOG.md)
- [来源与许可](THIRD_PARTY_NOTICES.md)

## 开发

```sh
npm ci
npm run build
.venv/bin/python -m pip install --find-links vendor/wheels '.[dev]'
.venv/bin/python -m pytest -q
```

前端资源随包分发。服务启动时同时加载对应的前端和后端，构建或升级后需在任务结束后重启服务。开发测试不调用真实模型或 Tavily。
