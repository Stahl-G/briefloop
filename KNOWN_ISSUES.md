# 已知边界与问题反馈

适用于 0.22.3。这里区分当前能力和仍需人工核对的部分。

| 范围 | 当前边界与处理方式 |
| --- | --- |
| 平台 | 桌面支持 macOS Apple Silicon 与 Windows x64；Linux 仅有 WebUI/离线 CI。桌面包未做 macOS 签名、公证或 Windows 代码签名，系统可能提示未识别的开发者。安装与平台验收范围见 [桌面指南](docs/桌面安装与更新.md) 及 [Windows 说明](docs/windows.md)。 |
| 桌面更新 | 由用户在“设置 → 版本更新”中检查、下载并发起安装，不在退出时静默安装，也不降级。macOS 更新包校验后需退出 App 并在 Finder 中替换应用；Windows 交给原生安装程序。差分下载的节省取决于版本间变化，首次或无缓存时仍可能完整下载。 |
| 内容方法 | 已接入通用报告、商业报告（行业周报／月报、工作进展周报、决策分析）、证券研究（事件点评、公司研究、行业跟踪）与会议纪要。学术论文、上市公司年报、政府公文和合同目前只有 Word 版式，没有专门内容流程。 |
| 首次使用 | 欢迎页可在空工作区导入合成示例，不需要模型或搜索 Key。实际生成、独立审阅和学习需要自己已认证的宿主与模型。 |
| 搜索渠道 | 新工作区默认 Tavily 优先，需要 API Key；博查、智谱搜索需用户启用并配置 Key；DuckDuckGo 无需 Key。宿主自带搜索取决于实际工具与权限。额度和费用以各服务账号为准。 |
| 来源读取 | 扫描 PDF、图片和复杂表格可能需要视觉核对；读取失败保留缺口。仅有搜索摘要不算已读取全文。网页上传来源时，PDF 单文件上限 100 MiB，其他文件 18 MiB；大 PDF 的正文抽取在上传请求内完成。Agent 发起的网页抓取和来源复查只访问公网地址；用户在界面上添加、重试或复查的网址可访问内网，但不读取本机回环与链路本地地址。配置 HTTP(S) 代理时由代理解析域名。 |
| 数值检查 | 只比较已明确绑定的正文数值和指定来源位置，支持的币种/单位可规范化。不完整、无法定位或不支持的绑定显示“未检查”；没有覆盖任意正文中的所有数字。已确认不匹配会阻断正式交付。 |
| 事实核查 | 可选。只对绑定正文的公开主张候选补查中性与反证来源，结果为支持、矛盾、证据不足或未知，再由独立 Reviewer 核对。未列入候选的内容不代表已核实；旧核查结果不用于修改后的版本。 |
| 语义与覆盖 | 主体、期间、口径、因果、预测和遗漏需要 Agent / Reviewer 核对。证据匹配和交付条件不构成正确率保证；没有登记缺口或冲突，也不说明检查完整。 |
| 跨段与来源类型 | 尚无对任意文本的自动跨段同指标一致性检查，也没有按来源类型自动裁决真伪。可比较结构化行业记录，登记分歧，并保留双方原文交由 Reviewer 核对。 |
| Reviewer | 受限能力取决于宿主实际隔离，不能用“只读”提示词代替权限。目前 Opencode CLI 和 BriefLoop 内置引擎（实验）声明了受限独立审阅（Opencode 的条件见 README）。执行后端不支持时，可在设置中为独立审阅单独选择其中之一，审阅改用所选后端和模型，事实核查与正式交付照常进行；不选时，事实核查在开始前就被拒绝，企业内部报告仍可生成、评分标为普通评分，正式交付需先完成独立审阅。内置引擎尚无凭据界面，模型凭据来自本机环境变量或 pi 的 auth.json。缺能力时不会降级为已审阅。 |
| 宿主与 Node | Codex / Opencode 原生通道不依赖 bridge 的 Node。WebUI 下其余宿主需要 Node.js 20+；桌面 App 复用 Electron Node。WebUI 可用 `BRIEFLOOP_NODE=/absolute/path/to/node` 指定。命令能找到不证明账号登录和模型调用成功。同一对话可切换下一回合的宿主，已开始的任务保持原宿主。 |
| Word | 用户模板暂限单节 DOCX；复杂合并单元格、浮动对象、跨节布局不保证保真。不同阅读器字体、分页和目录页码刷新可能不同，正式使用前打开实际导出件。 |
| 工作簿 | 能读取表格和复用图像，但不是在线 Excel，也不导出可编辑的复杂工作簿。 |
| 学习 | 保存反馈和使用已启用技能不调用模型。自动学习默认关闭；开启或手动启动前会显示调用上限（每轮最多 3 份历史报告、每份基线与候选各试写一次，另有整理、提案与比较回合，轮数和执行后端按设置），确认后才会在后台调用模型，费用以账户实际计费为准。旧版本升级的工作区（含已排队的学习批次）需重新确认一次；调高轮数、更换执行后端或模型后会暂停到再次确认，批次按确认时的试写上限执行。上限计的是试写次数，不含宿主内部子 agent 回合或 token 数。Agent 只能把上限报给用户，由用户确认。候选满足比较策略后自动启用，保留旧版和回退；Wiki 是经验，当前任务通过启用技能使用经验，不把 Wiki 当本期事实。尚无可推广的净收益结论。 |
| 运行 | 关闭浏览器后后台任务继续；关闭本地服务或电脑会中断。同时生成报告数默认 4，可在设置中调整；独立审阅和后台任务另外计算，同时进行的模型调用可能多于该数值，费用随之叠加。定时报告只在工作区服务运行时执行，离线错过的时点跳过、不补跑。无人值守批量、局域网团队服务和移动端尚未提供。 |

遇到问题先保留工作区，在新空目录用合成材料复现。记录版本、系统、宿主、步骤、预期与实际现象；按 [CONTRIBUTING.md](CONTRIBUTING.md) 只分享已检查的必要日志。安全问题按 [SECURITY.md](SECURITY.md) 私下报告。
