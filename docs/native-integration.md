# Native 集成状态（0.24.0 准备）

当前是开发集成，尚未发布0.24.0。产品版本暂沿用主干0.23.1，前端共同修订和发布验收完成后再统一升版。

## 分支与旧 PR

集成分支 `codex/native-integration-0240` 保留 Native 开发历史，并合入 `main@15d2ccc0`。三个早期PR的头提交均已包含：

| PR | 原职责 | 已包含的头提交 |
|---|---|---|
| #754 | 受限Native Reviewer与引擎基础 | `071b9b29` |
| #755 | Reviewer独立后端选择 | `14cf9684` |
| #756 | 审阅评测及角色扩展 | `4c31b30a` |

后续主Agent、Scout、Analyst、版本化写作和共用装配在同一集成分支维护，避免继续将早期三个PR当成全部Native功能。旧PR保留讨论记录，集成版合并前不宣称原PR已发布。

## 本轮收口

- `1ff42782`：结构有效但证据定位失败时保留首次正文，返回明确的未保存证据状态和补交入口；未知会话绑定不再诱导写作者自行创建身份。
- 保留0.23.1的共享推理档位和按模型目录显示Codex档位；Pi显式effort先设定并核对实际生效，保留包含SDK等待的计时。
- Native使用自己的模型目录读取档位，不经兼容宿主桥接；确认模型时保留Native variant。目录上下文与实际执行采用同一显式覆盖/默认规则。SDK仍固定0.85.1，未新增依赖。
- 设置入口分为“兼容宿主”和“BriefLoop 内置引擎”。原API入口默认进入Native配置；仅打开配置不会切换已保存引擎，明确选用时同步下一回合的引擎与模型。已有OpenCode配置不迁移或删除。
- 保留主干的Windows工件读取与LF校验修复；前端、桥接与Native包从源码重建。

## 验证与边界

相关后端91项通过；前端77项通过、1项跳过；引擎/桥接64项通过；Native类型检查、前端及Native构建一致性检查通过。wheel与sdist已构建，并核对内置引擎、档位目录、全部角色提示词和前端工件。wheel在仓库外独立环境安装后实际启动服务，静态页面、state、Native模型目录及Pi档位接口可访问，服务正常退出；未提交模型请求。模型调用采用本地脚本服务验证协议，这不是新增真实模型质量实验。

此前真实报告与性能实验保留原冻结工件，结果不归入本次新提交；写作接口仍按任务选择 `writer_input_v1`，不默认切换。最新共用装配试验没有证明稳定提速或质量提升。

前端布局与onboarding仍待用户共同修订。尚未验收当前集成版本的Mac/Windows安装和升级，没有发布PyPI、桌面或网站；PDF排版按用户决定暂缓。测试、包检查或旧候选验收不代表本版已发行。
