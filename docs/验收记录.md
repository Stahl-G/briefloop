# 真实宿主验收记录

这里记录在真实 CLI 宿主上跑过的端到端验收：做了什么、观察到什么、没有覆盖什么。只记录实际执行过的运行，不把源码分析或替身测试写成验收。

## 2026-09-16：Opencode 并行审阅与正式交付

环境：macOS 15（Apple Silicon）；opencode 1.18.30；执行后端 `opencode`，模型 `opencode/muse-spark-1.3-contributor-free`；BriefLoop 源码为当时的集成分支；临时工作区，两份合成材料（各一段中文文本），离线任务（`allow_web=false`），研究深度 quick，目标 350 字。

| 步骤 | 观察到的结果 |
| --- | --- |
| 两份报告同时生成（`max_reports=2`） | 04:11:06 同时开始，A 04:15:45 完成，B 04:19:57 完成，时间窗重叠 |
| 企业内部报告 A 的独立审阅 | 生成过程中自动运行并完成（04:14:43 → 04:15:19），Reviewer 只读固定核查包 |
| 两份报告同时独立审阅 | 04:19:59 两个审阅任务同时处于 running |
| 同一报告再次请求审阅 | 返回正在运行的同一个任务，没有并行开第二个 |
| 取消其中一份 | B 在 04:20:04 取消（"任务已停止，已生成内容保留"），A 不受影响，04:20:37 完成 |
| 正式交付门禁 | A 的 `eligibility.eligible=true`，无阻断项；3 条提示（1 条缺口披露、2 条数值未检查） |
| 正式交付 | 04:21:30 → 04:21:32 完成，产出 `report.docx`（36 KB）、`manifest.json`、`records.json` 和核查包；manifest 绑定 `review_id` 与 `review_fingerprint` |
| 交付件内容 | 正文分节、逐句带 `[1]` 引用、文末来源列表指向 `synthetic-A.txt` |

未覆盖：

- 本次审阅没有产生 finding，所以"处理审阅问题后再交付"（`/api/review-response`）这一步没有被实际执行。
- 合成材料短、报告 350 字、模型为免费档；这不能代表长报告、多来源、联网研究或付费模型下的表现，也没有测量 token 与费用。
- 没有做事实核查（`fact_check`）路径、定时报告路径和桌面 App 内的操作。
- 取消发生在审阅开始约 5 秒后；没有覆盖长时间运行后取消、或宿主异常退出的情况。

## 2026-09-16：桌面 PDF 导出（真实 Electron）

环境：Electron 44.3.0 / Chromium 152.0.7977.78（与 `desktop/electron/package.json` 锁定版本一致）。运行 `npm run verify:pdf`，在真实 Electron 主进程里执行与产品相同的导出代码。

结果：生成真实 PDF（`%PDF-1.4`，2 页，约 56 KB）；页面内的外部图片与 `fetch` 均未发出请求；`javascript:false` 下脚本无法执行；导出后无残留窗口与临时文件；取消保存不写文件；并发导出被拒且第一份仍完成。人工用 `pdftotext`／`pdfinfo` 与渲染页面确认标题、表格、内联图片正常，被拦截的外部图片显示为占位符。

未覆盖：没有在打包后的桌面 App 里用鼠标点击导出，系统另存对话框的外观与 Dock 行为未验收。

## 2026-09-16：内置引擎与 Opencode 同包同模型审阅对照

环境：macOS（Apple Silicon，接电源、开盖，`caffeinate -i`，运行期间 `pmset` 无睡眠记录）；Node 25.6.1；opencode 1.18.30；模型 `deepseek/deepseek-v4-flash`，推理档 high，DeepSeek 官方 API。两个运行时同时启动，各自复制同一个真实工作区（企业报告验收工作区），审阅同一冻结版本 `brief_a56c592a9aaf461d_r1`（它有 5 条针对本版的作者回应）。HOME 指向空目录，模型凭据只来自 `DEEPSEEK_API_KEY` 环境变量；代理环境变量已去掉。脚本：`native-engine/experiment_ab.py`。

| | 内置引擎 `briefloop-native` | `opencode` |
| --- | --- | --- |
| 结果 | 首次回复未通过结构校验（`requirement_checks` 多出 `basis` 字段），按校验错误重答一次后接纳，review `complete` | 首次回复即接纳，review `complete` |
| 墙钟时间 | 364 秒（模型请求合计 359 秒） | 252 秒（模型请求合计 244 秒） |
| 工具调用与模型请求 | 45 次 `packet_read`、1 次 `packet_list`，共读回约 87 万字符；28 次模型请求 | 34 次 `read`，共读回约 79 万字符；25 次模型请求 |
| 生成 token（含思考） | 7.05 万，其中思考 4.53 万 | 3.90 万，其中思考 2.90 万 |
| 回应核对 | 5 条：4 条 resolved、1 条 unresolved | 同样 5 条，逐条结论一致 |
| 主张核对 / 条款核对 | 6 条均 supported_for_scope / 21 条 | 6 条均 supported_for_scope / 21 条 |
| 发现 | 7 条（2 major、5 minor） | 2 条 minor |
| 未核验事项 | 4 条 | 5 条 |
| 总评 | 建议修改 | 建议修改 |
| 费用（各自价目表） | 0.082 美元（未缓存输入 34.5 万、缓存读 489 万 token） | 0.078 美元（未缓存输入 27.7 万、缓存读 421 万 token，取自 opencode 会话记录）；两边价目表不同，按同一份价目表换算，内置引擎贵约三到四成 |

耗时差异的构成（按两边会话记录逐次请求核对）：引擎自身开销约 1 秒，读文件耗时可忽略；两边生成速度相同（长回复约 230 token/秒），多出的时间几乎全部来自多生成的 3.1 万 token。其中约 1.1 万是结构校验失败后整份 JSON 重写（36 秒）；约 0.5 万是发现更多导致的最终 JSON 更长；约 1.6 万是审阅中途思考更多（3 次长思考分别用了 54、39、33 秒）。推理档位不是原因：opencode 会话记录的 variant 为 `default`，但对 DeepSeek 官方 API 单独对照（同一问题各 3 次），不传 `reasoning_effort` 与 `high` 的思考量相当（均值约 1.2 万与 1.0 万 token），同一设置下单次思考量可相差 2.4 倍。

两边共同发现：8 月 20 日、24 日的量比引用了工作簿，但数值只在图表数据文件里；正文主张绑定只覆盖少数段落。内置引擎另外报告了若干问题，其中一条已人工核对属实：业绩公告写 2025 年末现金及受限现金 5,890 万美元，电话会记录第 92 行写 “[ $85.9 ] million”，报告没有登记这一差异。图像输入两边不对等：opencode 的首条消息带了 4 张报告图（PNG 附件），DeepSeek 是否实际接收未核实；内置引擎不发送附件（`NativeHarness` 收到的视觉来源只做存在性校验），它用 `packet_read` 读图，pi 按该模型为纯文本丢弃了图片，于是把“附图未目视核验”列为核心未核验事项。另外，任务提示是为 Codex/opencode 宿主写的，多处与内置引擎不符（声称图片会作为附件发送、“只有 read 工具”、绝对路径、长行截断说明），本次未改提示。

未覆盖：

- 每边只跑了一次，发现条数、思考量和耗时的差异都不能当作稳定结论；除上面那一条以外，其余 major / minor 发现未逐条人工核对。
- BriefLoop 请求的 variant `high` 在 opencode 会话里记为 `default`，原因未单独排查；对 DeepSeek 没有影响，对默认档位不同的 provider 可能有影响。
- 没有测支持图像输入的模型，也没有测长时间卡流后在同一会话里重试的真实场景（替身测试已覆盖）。
- 内置引擎尚未出现在界面中，本次通过脚本直接排入审阅任务，没有经过页面操作和正式交付。

## 2026-09-17：内置引擎与 Opencode 同批 5 对 5 审阅对照

目的：判断内置引擎能否作为推荐的独立审阅后端。四条标准在运行前定好：被接纳比例不低于 Opencode；耗时中位数不超过 Opencode 的 1.1 倍；按同一价目表算的平均成本不超过 1.1 倍；两个已人工核实的问题检出次数不少于 Opencode。

环境：macOS（Apple Silicon，接电源，`caffeinate -i`）；opencode 1.18.30；模型 `deepseek/deepseek-v4-flash`，推理档 high，DeepSeek 官方 API。10 个进程在 13:42 同时启动，每个复制同一工作区、审阅同一冻结版本 `brief_a56c592a9aaf461d_r1`，各用独立的空 HOME，并预置 opencode 模型清单。凭据只来自环境变量。源码冻结在提交 fbd6c38e：内置引擎提供包内搜索、分段读取、主张追溯、计算和带校验的提交，系统提示词分层装配。两边共用的审阅要求点名了“图上文字与数据对照”和“同一指标跨来源核对”。脚本：`native-engine/experiment_ab.py`。成本按输入 0.22、输出（含思考）0.66、缓存读 0.007 美元/百万 token 统一计算；opencode 的用量取自它自己的会话库。

两个已知问题：
- **图表脚注矛盾**：图 `fig_dc483082d36b4165` 的 PNG 脚注写“8 月无单日量比≥1.5x 的放量交易日”，与同图标注、data.csv 和正文“3 个放量日”矛盾。
- **现金口径不一致**：2025 年末现金及受限现金，业绩公告写 5,890 万美元，电话会记录写 [ $85.9 ] million，正文未交代差异。

脚本按文本匹配标出检出，下面每条检出都已逐条读原文确认；未标出的审阅中，也没有以其他说法提到这两个问题。

| | 被接纳 | 耗时中位数（均值） | 同表平均成本 | 模型请求（均值） | 生成 token 均值（含思考） | 图表脚注矛盾 | 现金口径 | 结构纠正轮 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 内置引擎 | 5/5 | 388 秒（403 秒） | 0.136 美元 | 54 | 6.7 万 | 4/5 | 1/5 | 0 |
| Opencode | 5/5 | 341 秒（337 秒） | 0.143 美元 | 34 | 5.2 万 | 1/5 | 0/5 | 0 |

按预定标准：被接纳比例持平；成本为 Opencode 的 0.95 倍，通过；两个已知问题的检出次数更多，通过；**耗时中位数为 Opencode 的 1.14 倍，超过 1.1 倍，未通过。** 因此内置引擎继续作为实验选项，不作为推荐或默认的审阅后端。

多出的时间主要来自更多轮模型请求（平均 54 对 34）和更多生成 token（6.7 万对 5.2 万）。内置引擎平均每次读文件 51 次、搜索 19 次，Opencode 读 40 次。有一次内置引擎第一次提交被结构校验退回，只改出错字段后通过，没有触发整份重写。

未覆盖：
- 只有一个核查包、一个模型，每边 5 次。
- 除两个已知问题外，其余发现没有逐条人工核对，发现数量不作为质量结论。
- 本次共用审阅要求比上一次对照多了两类核查说明，结果不能和 9 月 16 日那组直接比较。
- 没有覆盖支持图像的其他模型，也没有经过页面操作和正式交付。

## 2026-09-18：内置引擎加入“成批查询”工作方法后复测（只跑内置引擎）

上一轮的会话记录显示：内置引擎约七成的模型请求只调 1 个工具，两次调用之间思考很长，读取本身并不重复。因此在 Reviewer 角色提示词里加了一段工作方法（提交 fdfa3858）：先列核查清单；互不依赖的查询放在同一次回复里并行发出；优先精准定位；两次查询之间不复述已读材料；清单核完即提交。工具说明开头也写明可以并行调用。Opencode 侧没有任何改动，所以只重跑内置引擎，沿用 9 月 17 日那 5 次 Opencode 作为基线。

环境与上一轮相同（同一核查包、`deepseek/deepseek-v4-flash` high、DeepSeek 官方 API、独立 HOME），只有一处不同：这次 5 个进程并发，上一轮是 10 个。provider 负载不同，耗时可能偏向对内置引擎有利，所以同时列出不受负载影响的模型请求轮数。

| | 被接纳 | 耗时中位数（均值） | 模型请求均值 | 生成 token 均值 | 同表平均成本 | 图表脚注矛盾 | 现金口径 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 内置引擎（本次） | 5/5 | 340 秒（329 秒） | 35.6 | 6.5 万 | 0.112 美元 | 1/5 | 3/5 |
| 内置引擎（上一轮） | 5/5 | 388 秒（403 秒） | 54 | 6.7 万 | 0.136 美元 | 4/5 | 1/5 |
| Opencode（基线） | 5/5 | 341 秒（337 秒） | 33.6 | 5.2 万 | 0.143 美元 | 1/5 | 0/5 |

检出都已读原文确认。按预定四条标准：被接纳比例持平；耗时中位数为 Opencode 的 1.00 倍；成本 0.79 倍；两个已知问题的检出合计 4 次，Opencode 为 1 次，两项各自都不少于 Opencode。四条都通过。

变化与代价：
- 模型请求从 54 降到 35.6，只有 1 个工具调用的轮次明显减少。这项不受并发数影响，说明提速主要来自成批查询。
- 现金口径冲突的检出从 1/5 升到 3/5，但图表脚注矛盾从 4/5 降到 1/5，降到和 Opencode 一样。新的工作方法强调搜索和精准读取，核查清单里没有点名“随消息附上的报告图”。

未覆盖：
- 每组 5 次、一个核查包、一个模型。
- 两轮之间并发数不同。
- 其余发现没有逐条人工核对。

## 2026-09-18：切片评测与内置引擎审阅档位

### 评测方法

- **切片**：`native-engine/slice_workspace.py` 从真实报告中切出一章，只保留该章引用的来源，并删去其余主张、历史版本、审阅记录和执行记录，要求改写为“只审这一节”。切片存放在仓库外（`~/Developer/briefloop-eval/slices/`，清单 `manifest.json`），每次审阅约 0.3 元，整份报告约 0.8 元。
- **植入问题**：`native-engine/seed_generic.py` 按随机种子在任意报告中植入 7 类问题：改数字、改百分比、改日期、换量级、去掉“预计”、换期间用词、编造句子。每个植入值在植入前的正文和来源中都不存在；审阅点出这个新值才算检出。用 20 份未植入的旧审阅按三组种子做了 315 次判定，误判为 0。
- **分组**：调优集 4 个切片（验收报告、reader 各 2 章）；留出集只在做判断时使用，用过或看过会话记录后即不再用于判断。
- **运行**：`native-engine/eval_slices.py` 以固定并发运行，每路使用独立 HOME，凭据只来自环境变量；汇总各类问题检出率、耗时中位数、请求轮数、token 和同表成本。模型均为 `deepseek/deepseek-v4-flash`（DeepSeek 官方 API）。

### 过程中找到并修复的问题

| 问题 | 影响 | 修复 |
| --- | --- | --- |
| 接纳规则允许 status=complete 而核查列表全空（一次审阅只在 summary 写了问题） | 两个后端 | complete 必须 coverage_scan_complete=true，且每个 requirement_item 都有核查 |
| 任务没列出 requirement_id，报错不点名 | 退回后整份重写 | 列出 ID；报错点名；`submit_review` 支持只重交出错字段 |
| 内置引擎提交时本地 JSON Schema 比正式接纳更严（不认字段别名） | 白跑一轮修正 | 有正式接纳时只以它为准 |
| BriefLoop 调用 Opencode 时每条消息都没带用户选的 variant | 用户选的档位从未生效 | 每条消息都传 `variant`（opencode 1.18.31 接口支持） |
| 工作方法引导“逐个数字搜索、读碎片” | 工具调用多、每读千字思考翻倍 | 改为按来源一次读完相关部分 |

### 档位对比

同一模型下，内置引擎在 high 档稳定比 Opencode（high）多想约 50%，检出相同或更全。降到 low 档后：

| 集合 | 内置引擎 | Opencode | 被接纳 | 检出 | 耗时中位数 | 同表成本 |
| --- | --- | --- | --- | --- | --- | --- |
| 调优集（8 次对 8 次） | low | high | 8/8 对 8/8 | 39/40 对 40/40 | 96 秒对 107 秒 | 0.038 对 0.045 美元 |
| 留出集（toyo-daily 的 6 月周报、9 月周报，4 章，8 对 8） | low | high | 8/8 对 8/8 | 22/24 对 22/24 | 74 秒对 98 秒 | 0.022 对 0.031 美元 |

留出集上内置引擎在 8 组配对中都更快，速度约为 Opencode 的 0.76 倍，成本约为 0.71 倍；两边漏掉的是同样两处。因此内置引擎审阅的默认档位设为 low（用户在设置中选择的档位优先）。

注意：DeepSeek v4 flash 只支持 low/high/max。pi 会把不支持的档位（如 medium）自动换成更高的 high，界面应只列出实际支持的档位。

未覆盖：
- 植入问题偏容易，两边检出都接近满分，还不能比出细微的质量差距。
- 每组 8 次，耗时波动可达 ±20 秒。
- 只测了一个模型。
- 切片不含长报告才有的跨章节一致性问题和历史回应复核。

## 待执行：Windows 桌面生命周期（#734）

需要一台 Windows 11 x64 和打包后的桌面版（后端与桌面壳同版本）。每步记录 BriefLoop 版本、Windows 版本、宿主 CLI 版本，以及进程列表或截图。

1. 新建空目录并打开为工作区；欢迎页能检测到运行环境。
2. 在材料里粘贴一个网址读取：成功保存来源，过程中不弹出命令行窗口，任务不停在"正在读取"。
3. 上传一份 10 页以上的 PDF：抽取完成，能看页图。
4. 设置 → 运行时检测：列出本机已安装的宿主与版本，未安装的标为未安装。
5. 切换到另一个工作区目录：原工作区后台停止，新工作区打开，原窗口内容已保存。
6. 生成一份短报告；生成过程中在任务管理器里确认子进程（python、宿主 CLI、必要时 curl / pdftotext）存在。
7. 用任务管理器强制结束桌面进程：确认后台 Python 服务及其子进程在数秒内退出，没有孤儿进程残留（这正是 #718 / #734 的所有权管道要保证的）。
8. 重新打开同一工作区：未完成的任务显示为已中断可恢复，正文与来源都在，恢复后能继续。

判定：第 2、3、4、6 步都不因为子进程继承桌面所有权管道而卡住；第 7 步没有残留进程；第 8 步没有丢内容。任何一步失败都记录当时的进程列表和日志（按 CONTRIBUTING.md 只分享已检查的必要日志）。

## 2026-09-19：内置引擎的 Evaluator 与学习角色（切片与真实反馈）

内置引擎新增运行器声明的工具（Python 定义、引擎转发）和三个角色：Evaluator、Wiki Maintainer、Skill Proposer。对照方式与 Reviewer 相同：同一模型，内置引擎用自己的默认档位 low，Opencode 用 high；各跑多次。

**Evaluator，deepseek-v4.1-flash（opencode-go），4 个开发切片 × 3 次**

| | 植入问题检出 | 耗时中位 | 同表成本 |
|---|---|---|---|
| 内置引擎 low | 58/60 | 100 秒 | 0.031 美元 |
| Opencode high | 56/60 | 154 秒 | 0.044 美元 |

同稿重复 3 次，四项分数最大相差 1 分，没有出现差 2 分。原稿上"达到要求/建议修改"在同稿 3 次之间翻转，12 个稿件×配置组合中有 3 个出现。植入 20 处事实错误后证据分从 4 降到 2，分析分在原稿和植入稿上几乎都是 3（见 issue 草稿：评分没有衡量洞见、信息密度与切题）。

**Evaluator，muse-spark-1.3（opencode-go），同样条件**

| | 植入问题检出 | 耗时中位 | 成本（muse 价） |
|---|---|---|---|
| 内置引擎 low，加规则前 | 47/60 | 61 秒 | 0.010 美元 |
| 内置引擎 low | 54/60 | 57 秒 | 0.012 美元 |
| 内置引擎 high | 57/60 | 109 秒 | 0.016 美元 |
| Opencode high | 54/60 | 116 秒 | 0.014 美元 |

加规则前的差距几乎都来自一次评分：模型在摘要里写出了全部错误，findings 却为空，被接纳后检出记为 0/7。本地真实工作区中 12 份"建议修改"的完整评分有 5 份同样没有 findings。现在共用评分要求写明"结论要求修改时逐条写成 findings"，内置引擎提交时强制检查并退回重写；外部宿主没有同回合修正，暂不因此判失败。muse 上 high 档没有让结论更稳（3 次一致的稿件：low 2/4，high 1/4），Evaluator 默认档位维持 low。

**Wiki Maintainer 与 Skill Proposer，deepseek-v4.1-flash，两个真实工作区的反馈 × 2 次**

| 工作区 | 后端 | 两步合计耗时 | 同表成本 | 经验条数 |
|---|---|---|---|---|
| first-live（改稿 + 评论） | 内置引擎 | 58–77 秒 | 0.012 美元 | 4、7 |
| first-live | Opencode | 181–299 秒 | 0.045–0.062 美元 | 4、5 |
| enterprise-evidence-smoke（改稿 + 审阅纠错） | 内置引擎 | 133–134 秒 | 0.044 美元 | 7、8 |
| enterprise-evidence-smoke | Opencode | 292 秒（1 次完成） | 0.081 美元 | 5 |

两边的每一步都一次通过 WikiSkill 的 collect 校验。内置引擎每一步是一个新会话，只能读冻结的交接包，建会话时即以真实句柄登记为 WikiSkill 子会话。第一次冒烟测试时，Proposer 把自己读包用的 packet 工具写进了 analyst 的候选技能；修正提示后 4 份候选都没有再出现。经验与候选技能的质量只做了抽查，没有做成对评价。

**成对比较（Evaluator 对比模式，学习门控），deepseek-v4.1-flash，两轮真实学习的比较输入 × 3 次**

| 输入（原宿主结论） | 后端 | 3 次结论 | 耗时 | 同表成本 |
|---|---|---|---|---|
| first-live（持平） | 内置引擎 low | 持平、持平、持平 | 13–22 秒 | 0.003 美元 |
| first-live（持平） | Opencode high | 更好、持平、持平 | 25–72 秒 | 0.009 美元 |
| enterprise-evidence-smoke（更好） | 内置引擎 low | 更好 ×3 | 22–26 秒 | 0.005 美元 |
| enterprise-evidence-smoke（更好） | Opencode high | 更好 ×3 | 63–73 秒 | 0.018 美元 |

内置引擎的比较结果先按 WikiSkill 门控与 learn() 的规则当场校验，再写入 comparison.json。每组只有一个案例、三次重复，只能说明结论没有明显漂移，不足以衡量判断质量。

## 2026-09-19：内置引擎的 Scout（首个联网角色）

Scout 在内置引擎上是独立会话：冻结的任务包，联网只经 web_search/add_url/extract_pages 三个受控工具并计入全 run 共享的研究预算，提交时过证据校验（excerpt 逐字、locator 可解析、按段落一条）。对照输入取真实工作区 toyo-daily-20260625-0701 的原生成任务：4 个研究槽位各跑一次，每腿把工作区重置为仅上传来源、新预算 8 次搜索/40 候选/10 页，两后端同模型（deepseek-v4.1-flash）同档位（low），携带原任务的搜索策略快照。

| 槽位（原 Scout 来源数） | 后端 | 耗时 | 同表成本 | 证据条目 | 来源数（新抓） | 与原 Scout 重合 | 证据校验错误 |
|---|---|---|---|---|---|---|---|
| scout-1（7） | 内置引擎 | 316 秒 | 0.073 美元 | 25 | 13（5） | 4/7 | 0 |
| scout-1 | Opencode | 160 秒 | 0.043 美元 | 14 | 14（7） | 4/7 | 14 |
| scout-2（8） | 内置引擎 | 260 秒 | 0.074 美元 | 29 | 11（5） | 2/8 | 0 |
| scout-2 | Opencode | 127 秒 | 0.038 美元 | 11 | 9（1） | 3/8 | 11 |
| scout-3（6） | 内置引擎 | 183 秒 | 0.055 美元 | 23 | 9（4） | 4/6 | 0 |
| scout-3 | Opencode | 110 秒 | 0.042 美元 | 10 | 10（5） | 5/6 | 10 |
| scout-4（12） | 内置引擎 | 138 秒 | 0.044 美元 | 16 | 7（0，纯本地） | 5/12 | 0 |
| scout-4 | Opencode | 122 秒 | 0.047 美元 | 15 | 11（2） | 5/12 | 13 |

中位数：内置引擎 221 秒/0.064 美元，Opencode 124 秒/0.043 美元。这是内置引擎第一个既不更快也不更省的角色。差价买的主要是可核查性：内置引擎每腿第一次提交都因证据校验被退回（excerpt 非逐字），修正后第二次通过，落盘结果 locator 全部可解析、摘录全部逐字；Opencode 没有同回合退回，落盘结果每腿带 10–14 处校验错误——绝大多数是 locator 不可解析（写成 `lines 18-24`、`lines 44-52, 110-116` 而非 `line 18-24`），scout-4 另有 11 处摘录非逐字，这些条目按现状无法回溯定位。两边的搜索预算都被正确计量（分别用 6/2/4/0 与 6/2/5/3 次，上限 8），与原 Scout 的来源重合度相当。

无网路径（reader-live，本地材料）内置引擎冒烟：复用原 Scout 全部 11/11 份来源，0 证据错误，402 秒/0.058 美元。

过程记录：第一轮双后端实验先后被 opencode-go 端点间歇性连接错误与 Opencode 旧 key 的周限额打断。前者促成了引擎重试从 3 次加到 6 次（2+4+…+64 秒，可扛约两分钟的提供方中断，引擎测试 29/29）；后者由用户在 Opencode 中换成新 key 后重跑，4 腿全部完成。之后又补跑一轮（两边无网 2 腿、Opencode 联网 4 腿）：同一时段两个后端都被提供方断连打断（Opencode 报 socket 意外关闭，内置引擎 6 次重试后仍是 Connection error），只有一条 Opencode scout-1 完成（176 秒，8 处校验错误，与上表一致），这轮不计入。原始数据：`~/Developer/briefloop-eval/scout/web-native.jsonl`、`web-opencode-agent-recovered.jsonl`（表中 Opencode 4 腿，从保留的工作目录重算）。另注：内置引擎按“一段原文一条证据”提交，证据条目约为来源数的两倍；Opencode 基本是一个来源一条。Python 全量 736 passed / 19 skipped。

## 2026-09-20：Scout 增量证据与有界读取（实现与单槽位真实复测）

针对 9 月 19 日对照中“整份抄写摘录、失败后整份重交”的问题，本轮增加内置 Scout 的 `record_evidence`：模型给稳定条目 ID、来源版本、单一行段和短逐字锚点，运行器从原文截取 excerpt。每条独立接纳，通过项在 SQLite 中保存；修正重用条目 ID，最终仅提交 gaps/search_summary/retrieval_notes，由运行器组装现有 ScoutResult。未处理的失败条目不能静默漏掉，移除需给原因并进入缺口记录。

证据暂存按报告、结果槽位和执行尝试隔离；工具写入串行执行，SQLite 事务防止并发覆盖；取消后的迟到提交被拒绝。唯一锚点才允许自动重定位，并返回新位置和实际摘录；重复锚点、来源版本变化或越权来源均不接纳。这证明定位和原文复制的完整性，不证明 facts 的语义判断正确。

小型任务包直接内联进任务消息（单文件不超过 6000 字符、合计不超过 14000 字符），其余文件明确要求读取。单次来源读取上限从 60000 调整为 24000 字符，可以继续读取。同步修改角色系统提示词、两种宿主的任务提示、共用产物要求、生成交接说明和 multi-search 技能；未给外部宿主增加它实际没有的 runner 工具。

**真实材料的确定性回放**：使用上一轮内置 Scout 已保存的 4 槽位、93 条证据，只读原工作区，不调用模型。从旧摘录取短锚点尝试重建，89 条直接接纳（其中 2 条重定位）；4 条因原行段超过 4000 字符拒绝。检查发现其中 1 份来源全文只有一行，长度 169868 字符，不能靠后续行号继续读取。因此增加 `source_grep` 命中附近片段与字符偏移、`source_read.start_char` 单行续读，以及证据的显式 start_char/end_char 范围。其余 3 条是多行段过宽，需要模型收窄到相关段落。本轮未自动丢弃表头或脚注来强行接纳；长行分段能力另有合成行为测试。

**验证范围**：最终全量 Python 回归 740 passed / 19 skipped / 16 subtests passed（111.44 秒）；Native 专项 27 passed。测试涵盖部分接纳、只修失败项、重复提交不重复追加、来源版本变化、唯一/歧义锚点、移除原因、并发记录、取消/新尝试隔离、长行读取及组装原结果契约。TypeScript 引擎代码未改，新工具通过已有 runner 协议声明；引擎测试 29 项通过。

**真实复测**：用户在交互终端隐藏输入凭据后，在 `9a4773c0` 上完成同一原任务的 scout-1。模型 `opencode-go/deepseek-v4.1-flash`、档位 low、预算 8/40/10；仍只复制原工作区。本次是一个槽位的一次运行，与历史样本比较，未重跑其他槽位或同时运行对照。

| 指标 | 历史内置引擎 | 本次内置引擎 | 变化 |
|---|---|---|---|
| 耗时 | 316.3 秒 | 188.6 秒 | 减少 40.4% |
| 模型成本（同一价格表） | $0.07281 | $0.05178 | 减少 28.9% |
| 输出 token（含 reasoning，不重复相加） | 40,828 | 22,662 | 减少 44.5% |
| 模型请求 | 17 | 24 | 往返增加，但单次输出缩短 |
| 最终整份提交 | 2 | 1 | 没有整份退回重写 |
| 证据条目 / 来源 / 新来源 | 25 / 13 / 5 | 31 / 11 / 3 | 条目数不是质量或覆盖率 |

本次模型成本拆分：未命中缓存输入 $0.02800、输出 $0.01496、缓存读取 $0.00882；未包含搜索提供商等费用。6 次增量记录中，第 3 次接纳 4 条、退回 2 条过长锚点；第 4 次只补交这 2 条成功。最后 31 条证据全部与其指定行段逐字一致，11 份来源哈希一致，证据暂存正常关闭，无待处理或被丢弃条目。最终产物已保存，结构性证据错误为 0。

**内容抽查未通过完整验收**：一条仅引用签署日期行的证据，其 facts 还概括了原文其他段落的法律依据与政策理由；这不是该引用段落能支持的内容。另一处二手表格与一手文本对一个产品的税率适用范围说法不同，31 条记录的 conflicts 却全部为空，检索小结还笼统称关键数值一致。此外，模型小结自述 4 次搜索，而运行器记录 6 次（5 次 ok、1 次 budget_exhausted）；消耗以运行器记录为准。来源哈希与逐字匹配证明摘录完整性，不证明上述语义结论正确。本轮没有执行写作、对照、独立审阅或报告导出，不能宣称整条报告链已经处理这些问题。

相较历史 OpenCode scout-1 的约 160 秒 / $0.0434，本次仍慢约 18%、贵约 19%，尚未达到交接中“不慢于、不贵于 OpenCode”的目标；历史 OpenCode 另有 14 处定位格式错误，不能据此断言两者质量相等。新来源从 5 份降至 3 份，且报告期内部分官方资料仍缺，不能声称覆盖无损。先处理 facts 与引用段落对应及跨来源差异提示，再扩到其余联网槽位与无网路径。外部宿主提示也有变化，后续须以实际代码版本记录同时对照；既有失败记录保留。

私有原始结果为 `~/Developer/briefloop-eval/scout/optimized-pilot-20260920-015344.jsonl`，对应 `-artifacts/` 归档保留产物、来源文本/哈希、工具记录和审查结果，不进入 Git。

**按调用链重新诊断**：不能把差距笼统归因为“校验更严格”。本轮原始 Native 会话的模型请求共占约 172 秒；6 次记录及最终接纳从模型结束到回执合计约 0.413 秒。Native 首轮输入 9,744 token，少于历史 OpenCode 的 15,835；平均每轮总输入也更少，但调用次数为 24 对 16，累计输入反而更多。可见输出为 12,398 对 15,243，推理却为 10,264 对 4,408。Native 31 段/86 条 facts 与 OpenCode 14 条来源/56 条 facts 的交接粒度也不相同，因此不是严格等工作量比较。

代码层面的低效点是强制先 grep 再 read，以及每次记录成功后回传完整摘录：本轮记录回执共 37,460 字符，最终摘录占 34,061 字符，重复进入后续上下文。第 8 次请求还出现 47,441 输入 token 全未缓存；其原因未证实。安装 SDK 的离线消息转换检查显示 24 轮旧消息前缀保持不变；实际打包引擎经假提供方两回合捕获，也确认 `reasoning_effort=low` 和稳定的 `x-opencode-session`，不能误判缺少缓存会话头。本轮美元数字按冻结价格表估算，不是实际账单；两宿主 catalog 价格不同，不能直接比较各自 runtime_cost。

下一步优先减少串行往返、重复回执和模型装配元数据，而不是继续堆提示词或取消校验；先用相同冻结材料与证据契约隔离引擎开销，再测试开放检索。上述诊断没有增加付费模型调用，也尚未将这些后续设计写入产品代码。完整逐请求分析在私有 `first-principles-diagnosis-20260920.md`。

### 2026-09-20：恢复检索策略对齐（后续用户决定）

用户要求先控制研究策略变量。本次撤回 Native 独有的“必须先 grep 再 read”和逐段立即记录要求，恢复按来源读取完整相关部分、按需定位、并行独立读取。两端读取上限恢复为 60000 字符；Native 的长行续读、来源哈希与确定性证据保存继续保留。OpenCode 的 `host_prompt` 已与优化前 `4c31b30a` 的函数逐 AST 对齐；共用生成提示和 multi-search 技能也恢复为该提交内容，不再向外部宿主注入 Native 的记录协议。

同时修正实验脚本的冻结策略错误：旧任务只有 `search_provider=tavily` 时，原脚本用当前默认 `resolve(primary='tavily')` 补出了宿主自带搜索。现在与产品 `search_policy.for_run` 一致，旧记录仍按原来的独占渠道解析；有明确新策略时逐项保留。多后端对照若包含 Native 不支持的宿主搜索，在调用模型前拒绝，不静默缩小或扩大允许渠道。

每臂记录任务、材料哈希/元信息、期间、研究要求、技能、渠道与预算的共同指纹，以及实际渠道、读取上限和结果传递方式；后臂条件变化会在模型调用前停止。对原真实 scout-1 任务做了 SQLite 副本上的离线预检：两臂共同指纹一致，有效搜索渠道均仅 Tavily，读取上限均为 60000，原工作区未改动。专项回归 **31 passed**，覆盖直接读取、冻结策略不扩权、能力不匹配拒绝、条件变化不派发模型及现有研究权限/交接。

这证明检索策略输入已对齐，不证明新配置下性能或报告质量改善。尚未重跑付费模型；旧数据与旧产物全部保留，作为当时各自配置的观察记录，不能作为已控制研究策略的引擎因果对照。Native 的增量 runner 与 OpenCode 的文件交接、结果接纳差异仍明确记录，不将它们冒充完全相同的工具实现。本次也没有同时进行回执压缩等其他性能改动。

实验脚本增加 `--prompt-key`（仅交互终端隐藏输入，传入进程环境，不写文件）与 `--output`（独占创建新 JSONL，拒绝覆盖历史结果）。只复制实验工作区，不修改原报告；没有切换前端默认后端，没有放开尚未接通的内置主链任务，也没有发布新版本。

### 2026-09-20：检索策略对齐后的单槽位成对复测

用户追加授权后，在冻结提交 `fc379b6d` 上完成 Native、OpenCode 各一次 scout-1。两臂同模型 `opencode-go/deepseek-v4.1-flash`、low 档、原任务材料及期间、Tavily 独占渠道、预算 8/40/10、读取上限 60000 字符；共同策略指纹相同。实验期间源码和原任务文件哈希未变化。没有继续修改研究策略或压缩回执。

| 指标 | Native | OpenCode |
|---|---|---|
| 保存结果 / 耗时 | 完成 / 128.5 秒 | 完成 / 179.8 秒 |
| 模型请求记录 | 17 | 18 |
| 未缓存输入 / 缓存读取 token | 107361 / 1350272 | 131637 / 1341696 |
| 输出 token（含推理） | 17199 | 18036 |
| 已记录用量的同表估算 | $0.04442 | $0.05026，存在漏计，见下文 |
| 受控搜索 / 候选 / 正文页消耗 | 6 / 40 / 4 | 5 / 36 / 8 |
| 证据条目 / 唯一来源 / 新来源 | 23 / 7 / 4 | 11 / 8 / 3 |
| 结构性证据校验错误 | 0 | 6，均为定位字符串格式不符合契约 |

本轮 Native 比 OpenCode 快 28.5%。但 OpenCode 有一次整份写入的 JSON 参数错误，随后自行恢复；该请求持续约 38.25 秒，确有生成内容，数据库却记录 input/output/reasoning 全为 0。因此 OpenCode 的用量并不完整；同表金额仅代表已记录部分，不能把 11.6% 的账面差异当成已证明的实际账单降幅。价格表为每百万 token：未缓存输入 $0.22、输出 $0.66、缓存读取 $0.007，不含搜索等费用。未追加请求来估算漏计量。

**内容抽查仍有差异**：Native 的 23 条摘录逐字匹配对应行段，7 份来源哈希正确，暂存已关闭、无待处理条目；一条过长锚点被明确拆分取代，原因保留。但部分 facts 超出本条引文支持范围，例如一条只摘录指引发布日期，却附带其他段落的处置期限；价格表引文还附带未由该来源证明的“首次”判断。工具输入和暂存记录确认这些行段由模型直接选择，本轮没有发生自动重定位。Native 的 conflicts 全为空；OpenCode 则记录了实施期限及卖方口径差异，不过这也不证明其所有判断正确。条目粒度不同（Native 44 条 facts，OpenCode 43 条），不能用 23 对 11 宣称质量翻倍。

这是一组开放检索观察：约束相同，实际检索词、网页、来源覆盖和缓存状态不完全相同；Native 增量接纳与 OpenCode 文件交接仍不同。能确认本轮 Native 不再更慢，不能据单样本宣布稳定优于 OpenCode或完成整个报告闭环。本轮未运行 Analyst、独立 Reviewer、修订或导出，仍需解决 facts 与引文的语义对应及跨来源分歧遗漏。

先前外部终端的一次 Native 请求返回 401、零 token；替换终端进程时已经开始的 OpenCode 部分被中断，单独标为无效记录，不混入本表。正式两臂结束后，实验进程及其 Native/OpenCode 子进程均已退出。私有原始结果为 `~/Developer/briefloop-eval/scout/aligned-pair-20260920-030842.jsonl`，归档含两端产物、来源、哈希、工具记录、用量元数据及抽查说明；不含密钥或隐藏推理正文，不进入 Git。

### 2026-09-25：英文报告小型真实闭环

冻结提交 `9812347f`（`claude/english-reports`）。新工作区；后端为内置引擎，模型 `opencode-go/deepseek-v4.1-flash`，研究档位 quick，DuckDuckGo 独占渠道，未开事实核查，版式为“英文通用报告·品牌绿”，篇幅 500 / 650 词。需求用中文填写，只把报告语言设为英文。题目：2026 年 8 月中国新能源汽车出口。

同一配置先用 OpenCode 后端试跑，启动即失败，报错 `opencode serve 未就绪: Expecting value`（本机 opencode v2.0.14）。失败发生在本分支未改动的 OpenCode 宿主，未继续排查，记为环境问题。

结果：

- **成稿**：生成、评价和一次自动修订共约 21.5 分钟，登记来源 22 份。首稿评价为“建议修改”，列出两处引用挂错来源、一处无来源的盈利判断、一处 CPCA 口径量级未说明。修订稿评价为“达到要求”，剩余三处轻微问题，其中包括 CPCA 缩写首次出现未展开。
- **写作与记录**：正文、标题、表格和图注均为英文。CAAM 全口径与 CPCA 乘用车口径分开写，“52.6万辆”写作 526,000 units。research_notes 和评价说明仍为中文。
- **篇幅**：修订稿按词计 649，上限 650。
- **Word 导出**：来源、目录、页码均为英文标签。导出文件中仍有中文的只有两处：来源原标题（按设计保留），以及封面上按原文打印的时间范围——本次验收填写的是中文“2026 年 8 月”。据此在英文模式下给时间范围输入框加了提示，并更新了文档。
- **数值检查**：修订稿 16 条数值绑定中机械核对 4 条（百分比），全部一致；其余 12 条是车辆数量。本分支随后加入 units / vehicles / 辆 / 台及其量级，复核这份稿件时发现：正文用不带单位的表格数字引用“68,431 vehicles”时会被误判为“不一致”，而这一项会阻断正式交付。已改为：数值与绑定值相同、单位只在表头或上下文里时，记为“单位未机械核对”；真正的量级换算错误仍判为不一致。

本轮没有运行独立 Reviewer 或正式交付，也没有开事实核查。私有产物保存在 `~/Developer/briefloop-eval/english-report-20260925-9812347f-native/`，不进入 Git。

### 2026-09-26：英文报告合并前修复

修复上传模板没有语言提示、或清除模板选择时把英文改回中文的问题。只有模板明确声明支持的语言时才同步报告语言；保留旧任务 `English` / `中文` 的归一化。普通 Word 与模板 Word 的 renderer 缓存标识均已更新，避免旧英文任务继续下载带中文读者标签的历史导出。

回归检查实际保存了两条路径下完整且哈希有效的旧缓存 Word，再以同一稿件、来源、要求和模板重新导出。两条路径均建立新导出并显示 `Sources`，随后复用新缓存；原稿、保存要求及旧文件字节保持不变。原主干 36 个中文模板及其 spec 共 72 个文件逐一比对，字节完全不变，没有重新生成。

Windows CI 上次失败来自启动验收将模板数写死为 36，新包实际包含 46 个模板。本次改为读取已安装包的内置模板清单，并同时检查名称全集和英文模板的语言提示，避免仅放宽数量而遗漏新模板。

本地验证：英文与导出行为检查 **13 passed**；前端检查 **115 passed / 1 skipped**，跳过项为实际 Electron 剪贴板验收；前端构建、构建一致性和设计约束检查通过。Windows 原生检查须以本提交推送后的 CI 为准，Mac 上没有将跳过项记为通过。

修复提交 `8a8d8081` 的 [Windows 安装包检查](https://github.com/Stahl-G/briefloop/actions/runs/36236929080/job/108390296622) 随后通过。再接入包含 #794 / #800 的主干 `90994ff3`，保留 OfficeCLI 取消处理与原子预览缓存改动；唯一冲突为生成的前端 bundle，按合并后的源码重建。组合检查（英文、Word 导出、OfficeCLI）**33 passed**；前端 **117 passed / 1 skipped**，构建一致性和设计约束仍通过。集成提交的全量 CI 单独执行，不沿用前一个提交的成功状态。

本轮未调用真实模型、未重跑独立 Reviewer、事实核查或正式交付，也未修改旧验收原稿。上一节真实模型结果仍只对应 `9812347f`；其 OpenCode v2 启动失败是后端版本兼容问题的线索，不能仅凭该次报错断言网络或用户环境故障。


## 2026-09-26：OpenCode 双版本与审阅模式集成

OpenCode 2.0.14 不再使用 1.x 的健康、会话与模型目录协议。本次保留 v1 传输并增加独立 v2 适配，模型、variant、目录和权限需回读一致才发送任务；不存在的旧会话不自动重建。v2 的 Provider 配置仍在宿主内完成。实际供应方边界显示，v2 后续指令条目修改并不等价于 v1 每轮 system，因此已开始会话的系统约定变更须明确新建对话，不能静默替换或重发。

按用户最新决定，审阅拆分为默认普通模式与显式严格模式。Codex、OpenCode 两代、BriefLoop Agent 可提供普通独立只读审阅；只有当前核查包专用工具已落实的 BriefLoop Agent 声明严格隔离。OpenCode v1 同样自动发现全局/祖先指令，旧文档对其完整上下文隔离的描述已纠正。两种模式沿用同一证据、版本、重大问题及交付门禁，新任务记录实际模式；历史缺字段仍显示未记录，不回写为严格。

集成英文报告与 Excel 主干后，定向 Python 检查 236 通过，前端 125 通过、1 跳过，构建一致性与共享设计检查通过。检查包括模式/后端去重、严格不复用普通结果、旧会话恢复、定时触发时采用当前设置而入队后冻结、相同重大问题两模式均阻断、Codex 内部任务实际出站只读/禁网络参数，以及当前轮最终 JSON 与进度消息分开保存。没有把最终结构化回复接纳当成语义审阅通过。

实际 macOS OpenCode 1.18.31/2.0.14 与自建本地合成供应方验证了认证、High 参数、读取/写入拒绝、中文消息与用量投影、空闲取消和自有服务清理；另有 v2 Harness→ChatStore 闭环。没有读取用户凭据，没有外部模型调用。这些不是完整报告、多子 Agent、长任务取消或 Windows 桌面验收。

在独立、暂停任务派发的合成工作区通过 Chrome 实际验收：普通/严格保存及刷新保持、OpenCode 严格模式明确不可用且不自动换后端、手选 Native 严格模式、Codex 普通模式模型/档位文案与 High 实际保存。工作区任务数为 0，没有触发模型审阅。冷启动的模型目录跨快照异常另行修复并保留初次失败观察。

目录修复后另跑 12 项协议检查通过（含首次不一致恢复与持续不一致拒绝）。真实 v2 冷启动公开目录顺序观测为 0 个 Provider → 494 个模型 → 4 个 Provider → 425 个模型；适配器完整重读一次后返回一致的 4 个 Provider / 425 个模型及公开档位，没有过滤丢模型或臆造 Provider，没有发送模型任务。

## 2026-09-26：0.25.0 Issue 收口与发行准备

- #730 补齐欧元/英镑、数量级股份、质量和电量单位；保留币种、功率/电量及百分比/百分点边界，不做汇率换算。SI 符号保留大小写，mWh/MWh、mW/MW、t/T 不会相互通过；非法或未知单位仍未检查。数值、覆盖、英文报告与软目标用时相关67项通过。
- #729 修复旧排队/运行任务被最近30条历史挤出；只限制非活跃历史，任务去重与排序保留。相关派发、取消恢复、正文按需、stdin、PDF缓存及导出行为16项通过。Issue中的完整列表分页和100/1000报告基准仍待做，不标全部完成。
- 前端125项通过、1项跳过；桌面70项通过、4项跳过；前端构建/共享设计及源码版本一致性检查通过。此处是源码候选验证，最终冻结提交、实际安装包、Mac原生运行和公开渠道校验随0.25.0发行记录保存，不将本段当作发行已经成功。
- #758/#759/#762作为已写成的研究归档和交接备忘结案，后续阈值、对照与独立验证仍在#760/#761/#763。#728/#729/#731/#732/#734/#736/#757/#764仍保留未完成验收或实施项。Windows原生验收不可用，0.25.0 Windows安装包暂缓；Mac本轮没有Developer ID签名或Apple公证。

### 2026-09-26：大文件与大工作区优化候选（未发布）

基线 `95437f4c`，独立分支 `codex/usability-next-20260926`，版本保持 0.25.0。本次加入来源流式接纳与后台读取、报告摘要分页和按需详情、Excel基础列宽/换行，以及本地文件任务标签。内部 trial 稿件继续可通过工具读取，不套用网页列表的 normal 模式筛选。

- **合成性能记录**：macOS，100/1000份报告，每份4个长 Markdown＋富文档版本，各9次。1000份热状态从119,653,012降至58,598字节，响应构造p50从547.08降至16.04ms，p95从574.04降至46.20ms；Store.rows调用55→25，双方篇幅计算均0次。后续补入来源初始计数和三个运行参数，会有少量固定响应增量。这不是生产SLO，全部活跃任务仍保留，来源等其他全局集合未在本轮分页。
- **100MiB PDF记录**：单页大灰度图合成PDF；原件哈希和提取文本一致。服务进程观测RSS峰值306,656→73,424KiB；新版父子同时观测峰值132,768KiB。旧子进程过短未采到，不能视为不占内存。原件接纳0.369→0.144秒，但完成读取0.369→0.552秒，隔离子进程有启动成本。40ms采样，不能代表所有复杂PDF或跨平台最高水位。
- **网页实际路径**：隔离56报告工作区，加载全部列表、搜索并打开热窗口外旧稿、正确版本选择、上一稿差异标记、编辑后自动保存。通过网页上传35页合成PDF并读取完成；损坏PDF的失败与重试独立记录保留。网页生成Excel有真实保存工件，任务显示本地文件处理。
- **恢复与边界**：针对性检查覆盖断线、接纳回执恢复、真实子进程取消、终态后晚到结果拒收、服务重启、无pdftotext回退，以及最终日志/清理异常后继续处理下一份文件。扫描PDF终态明确未OCR，不把原件保存当正文已可用。
- **验证**：本轮相关Python检查114通过、3 subtests通过；前端137通过、1项原有原生剪贴板检查跳过；构建及共享UI设计检查通过。独立审查找出的旧稿版本框、trial详情、进度摘要字段和PDF终态异常问题均已修复。不是全量旧回归或全平台桌面验收。
- **未验收**：Excel在WPS原生窗口的显示受Mac锁屏阻挡，未宣称通过；本轮没有发布、切换用户安装或关闭Issue。#757仅准备了有限成对质量试点，不据材料构造或Schema通过声称评价质量提升。

### 2026-09-26：本轮后续原生验收与有限质量试点

桌面恢复后，用WPS实际打开网页下载的Excel，下载文件与后台生成工件哈希一致。中文表格和多行说明显示正常，但目录长标题末字因行高估算偏紧被裁切。仅为换行字符串的宽度估算增加字体差异余量，列宽、数值与正文保持；renderer缓存版本更新。修复后重新打开sheets和single工件，目录末字及多行说明完整可见。原初版和问题观察保留；32项Excel行为检查通过，独立审查无新增发现。

最终冻结提交bc811295的100/1000报告基准各9次：100份响应11975212→59524字节、p50 65.11→10.34ms、p95 69.77→11.79ms；1000份119653012→59698字节、p50 563.44→17.22ms、p95 648.93→20.33ms。边界同上。Excel末次修复在模型试点全终态后进行，不改试点冻结。

#757试点改用OpenCode Go供应通道的DeepSeek V4.1 Flash，Native Evaluator入口，High、1M上下文、32768输出、自动压缩关闭，最多2并行。两章10对、两规则、两次重复共40任务全部保存，0终态失败；不是OpenCode CLI引擎对照。原GLM官方8次401及32未启动记录保留，不混算。当前规则/新锚点与构造预期一致19/20与20/20，仅一个简洁标签偏好差异；重复偏好一致9/10与10/10，四维重复最大变化均1分。中位耗时100.38与108.74秒。26任务发生28次工具拒收并在原会话修复，其中24次漏顶层reason。

40份公开结果逐稿AI复核已完成，发现读漏已保留条件、原建议与来源条件矛盾、修订建议越期等语义问题，也保留准确识别套话/离题和分支安装条件的正例。3对材料预先标记混杂，人工标注分母仍0；构造一致率不是检出率，未证明普适质量提升。正式评分规则不变，#757继续开放。全部材料、结果、metrics及分析留在本地私有验收目录，不上传运行记录或凭据。
