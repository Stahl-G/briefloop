# BriefLoop 开发约定

这是本地研究与报告工具，产出企业内部可读、可核查的报告。界面上的主 Agent 叫 BriefLoop：负责对话、研究规划、写稿与修订，模型用主链配置；Evaluator、Wiki Maintainer、Skill Proposer 三者独立运行，可分别指定模型。目标：重要结论可追溯，草稿持续可编辑，正式交付与独立审阅和证据版本绑定，通过既有 WikiSkill 学习已核实的修订。

- 若存在 private_planning/PLAN.md，以其中的最新用户决定为准；普通 clone 以本文件和 README 为准。旧项目的阶段、审批和测试约定不自动继承。
- 实现真实可操作的网页和后台 agent 执行，不把手动 JSON 或 mock 当成完成。
- 前端新增功能直接写进 `frontend/` 下的新模块（工厂函数注入依赖，参考 schedules.js、app-updates.js 的写法）；`app.js` 只保留全局状态、启动与组装，不再整块增长。
- 保留精简 SQLite ControlStore、来源、稿件版本、评分绑定和恢复；不复制旧 mandatory Auditor/Editor/Claim Ledger 流程。
- BriefLoop（主 Agent，代码中也叫 Orchestrator）和实际 agent 负责规划、研究、写作、评分；Python 负责工具、进程、存储、确定计算及既定选择规则。
- 用户原稿/修订可见；低分不隐藏稿件，普通工作稿始终可下载。正式交付不能豁免确定重大错误、核心证据缺口或未决重要冲突。Wiki 是经验，不是当期事实来源。
- Reviewer 独立只读核对实际产物、原件与本任务历史，不自行重跑分析、补搜或改稿；权限由选定宿主实际落实，缺能力不得用提示词冒充隔离。
- 复用现有 jobs/events/Store 和一次自动修订；来源正常更新与原稿错误分开。审计包由用户单独选择导出，不含凭据和隐藏推理，含各材料的定位与校验结果。
- 默认 Scout/Analyst 可演化，但角色/技能/评价接口可扩展。K 在报告反馈页调整，默认 1。
- 只保留必要测试，不追求覆盖率、千级旧回归或状态组合矩阵。保存、绑定、失败恢复、选择/回退以少量行为检查覆盖；最终一次小型真实闭环。
- 不覆盖旧仓库和用户改动；外部代码保留许可证与来源说明。
- 凭据/运行数据/详细计划不进 Git。公共安装不能依赖个人绝对源码路径。
- 普通本地实现及必要修复已获授权，不逐步请求批准；外发、发布和新付费服务另计。

## 版本与发布

- 产品版本以 `pyproject.toml` 为唯一输入；其余 Python、CLI、Web、Mac/Windows Electron 配置及锁文件随发行同步。检查从元数据读取目标版本，不写死某一发行号；正常测试里的旧版/新版场景数据可保留。
- 从同一冻结提交构建发行。共用后端 wheel 构建一次，CLI/PyPI 与 Mac/Windows 打包复用同一份工件；发布阶段上传已验证工件，不临时重建同版本的不同内容。
- 运行 `scripts/check_versions.py` 核对源码及实际 CLI、Mac App、Windows 程序、内置后端 manifest/wheel 和 PyPI 版本与哈希。跨平台原生结果可汇总；缺少或失败的证据必须显示未核验/失败，不能将源码版本相同当作安装包或本机已升级。
- 发行按准备版本与文档、构建、各平台必要启动/报告导出/更新验收、发布工件、更新下载页与正式更新索引的顺序推进。发布后从实际渠道重新核对；仅在各目标平台就绪后宣布统一发行。用户明确指定单通道发布时照办，同时标注其余渠道仍处于哪个版本。
- PyPI 与桌面发布可能部分成功。保留成功工件，只重试未完成部分；不覆盖已发布同名包、不重写旧发行来凑一致。凭据不进仓库或日志；自动发布优先采用配置好的 PyPI Trusted Publishing。
- 桌面使用固定版本与哈希的后端；不在启动时无条件安装 PyPI 最新版。后端独立更新需先明确壳/API兼容范围、新环境校验、切换与失败回退，未经实现和验证不宣称已经支持。
- 关闭 Issue 前逐项核对其验收标准，引用已合并实现及实际验证，记录未完成项。构建/CI通过或单个平台成功不自动代表整个 Issue 完成。
