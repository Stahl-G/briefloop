# Optional model provider configuration.
# The local MVP uses a deterministic mock provider and does not require keys.

MODEL_PROVIDER=mock
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
LITELLM_MODEL=

# Optional delivery connectors. Keep disabled unless explicitly configured.
FEISHU_ENABLED=false
SLACK_ENABLED=false
EMAIL_ENABLED=false

