# BriefLoop 界面设计系统（DESIGN.md）

范围：`src/briefloop/static/`（原生 HTML/CSS/JS，无构建框架）。本文件是界面重构的令牌与组件基准；改 UI 先改这里。

## 1. 布局骨架（三区）

桌面：

```
┌ 左导航 218px ┬──────── 主区 ────────┬ 右栏 340px（可折叠）┐
│ 对象导航      │ 顶栏 / 内容 / 编辑器 │ tab 面板            │
└──────────────┴──────────────────────┴────────────────────┘
```

- 左导航（`.session-sidebar`）：对象态入口 + 最近项；宽度 `--sidebar-width`。
- 主区：报告工作区为「顶栏 + 内容 + 编辑器」。正文列宽 `--chat-column`（聊天）/ 自适应（报告）。
- 右栏（`.rail`）：研究助手 / 来源与数据 / 检查结果 / 讨论 四 tab；可折叠为 0。

响应式：
- `≤1150px`：右栏换成覆盖式面板；导航保留图标+文字。
- `≤780px`：导航收成窄条；右栏变抽屉式覆盖。
- `≤560px`：导航只留图标。

## 2. 令牌

| 令牌 | 值 | 用途 |
|---|---|---|
| `--green` | `#006838` | 主色、链接、主要按钮 |
| `--green-hover` | `#145238` | 主按钮悬停 |
| `--ink` | `#1e2320` | 正文 |
| `--muted` | `#6a706b` | 次要文字 |
| `--paper` | `#faf9f6` | 页面底色 |
| `--line` | `#dedfd8` | 分隔线/边框 |
| `--surface` | `#ffffff` | 卡片 |
| `--soft` | `#f4f6ef` | 次要面板底色 |
| `--warn` | `#8a6d1a` | 缺口/注意（黄） |
| `--danger` | `#a5472e` | 冲突/错误（红） |
| `--radius` | `9px` | 卡片圆角 |
| `--radius-sm` | `6px` | 控件圆角 |
| `--radius-pill` | `100px` | 胶囊标签 |
| `--space-1..6` | `4/8/12/16/24/32px` | 间距刻度 |
| `--shadow-sm` | `0 1px 2px rgba(30,35,32,.06)` | 卡片 |
| `--sidebar-width` | `218px` | 左导航 |
| `--rail-width` | `340px` | 右栏 |
| `--chat-column` | `728px` | 正文/聊天列宽 |

字号层级：`11/12/13/14/15/17/23/30px`（caption/label/body-sm/body/h2/h1）；行高 1.6–1.9。字体沿用 `--font`，等宽 `--mono`。

## 3. 组件

- **顶栏 `.report-topbar`**：面包屑 + 标题 + 状态 chips + 右侧动作（版本、导出、⋯）。高度 ~64px，底部分隔线。
- **状态 chip `.chip`**：`已保存`(中性) / `评分中`(中性) / `已评分`(绿) / `有缺口`(黄) / `有冲突`(红)。仅当状态存在才显示。
- **分段 tab `.seg-tabs` / `.rail-tabs`**：文字按钮，选中 `--ink` + 底部 2px `--green`；未选 `--muted`。
- **右栏 `.rail`**：`tab 头 + 可滚动 pane`。pane 用 `[data-pane]` 切换，同一时刻只显示一个。折叠按钮在 tab 行右侧。
- **菜单 `.menu-wrap > .popover`**：点击展开，点外部/Esc 关闭；`role=menu`。
- **卡片 `.panel`**：白底 + `--line` 边 + `--radius`。
- **按钮**：`.primary`(绿底白字) / `.ghost`(无边框，悬停浅底) / `.outline`(白底描边) / `.wide`(占满)。
- **来源行 `.source-row`**：名称 + 状态徽标（已核验/待核验/读取失败/需视觉）+ 定位入口。
- **检查摘要 `.check-summary`**：一项百分比 + 冲突/缺口数。
- **发现 `.finding`**：折叠项，severity 用红/灰点；含 report_quote / requirement / evidence / suggestion。

## 4. 规则

1. 顶栏只放「对象级」动作；行内/上下文动作放编辑器浮层或右栏 pane。
2. 同一动作只出现一处；不要顶栏和右栏各放一个（评分只在右栏）。
3. 右栏默认常显，可折叠；折叠状态按工作区记住。
4. 任何"受控工具额度"文案不得写成宿主费用上限（见 plan v0.2 §7）。
5. 保留既有 `#id`（前端测试依赖），结构重构用 class，不轻易改 id。
6. 未实现的功能不放占位按钮（宁可少一个 tab，也不放空壳）。

## 5. 报告工作区映射

- 顶栏：面包屑(`#report-title`) · 状态 chips(`#report-status`) · `版本`(`#version-select`+`#version-history`) · `导出`(Markdown/Word/图文包/正式交付/审计包) · `⋯`(导入 Word 修订/导入 Markdown/按审阅修改/独立审阅)。
- 顶部 tab：`编辑`(默认) · `数据与来源` · `检查与评价`；`数据与来源/检查与评价` 直接切右栏对应 pane。
- 右栏 tab：`研究助手` · `来源与数据` · `检查结果` · `讨论`。
- 编辑器：格式工具条保留；`当前段落依据` 进检查 pane（后续版本改行内浮层）。
