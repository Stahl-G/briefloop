# BriefLoop 界面设计系统

**范围**:`src/briefloop/static/`(原生 HTML/CSS/JS,无构建框架)
**状态**:v2 · 最后更新 2026-09-18
**令牌实现**:`tokens.css`(本文件是说明,`tokens.css` 是唯一事实来源)

---

## 0. 本文档包含什么 / 不包含什么

| | 写在哪 |
|---|---|
| 布局骨架、令牌、组件、交互状态、跨页规则 | **本文件** |
| 单个功能的产品行为决策(删除语义、模型继承、额度口径……) | `DECISIONS.md` |
| 单个页面的完整交互流程 | 各功能 spec |

**判定标准**:一条规则如果换一个功能仍然成立,写在这里;只对某个功能成立,写在 `DECISIONS.md`。

> v1 的教训:产品行为规格(「检测到 CLI 不表示账号调用成功」)混进了设计文档,结果改 UI 的人不看,写逻辑的人找不到。见 §11 变更记录。

---

## 1. 令牌:两层结构

令牌分**原始层**(具体色值/数值)和**语义层**(用途别名)。**组件只允许引用语义层。**

```
原始层  --c-green-600: #006838
语义层  --color-primary: var(--c-green-600)
组件    .primary { background: var(--color-primary) }
```

为什么必须分两层——v1 的实证问题:

- `--green` 同时是品牌主色、链接色、主按钮底色,**以及**分类色标里「学术/资料」的前景色。换品牌绿会连带改掉一个内容分类。
- `--line` 同时是装饰分隔线和输入框边界。前者 1.34:1 够用,后者不满足可访问性下限(见 §2)。

一个原始值可以被多个语义名引用,这不是冗余——它让每个用途能独立演进。

### 1.1 色彩

完整清单见 `tokens.css`。语义层关键条目:

| 语义令牌 | 引用 | 用途 |
|---|---|---|
| `--color-primary` | `--c-green-600` `#006838` | 主按钮底、链接、选中态 |
| `--color-primary-hover` | `--c-green-700` `#145238` | 主按钮悬停 |
| `--color-text` | `--c-ink-900` `#1e2320` | 正文 |
| `--color-text-muted` | `--c-ink-500` `#6a706b` | 次要文字 |
| `--color-bg` | `--c-paper` `#faf9f6` | 页面底 |
| `--color-surface` | `--c-white` | 卡片、输入框 |
| `--color-surface-soft` | `--c-ink-050` `#f4f6ef` | 次要面板底 |
| `--color-border` | `--c-ink-200` `#dedfd8` | **装饰性**分隔线、卡片描边 |
| `--color-border-strong` | `--c-ink-400` `#8b918b` | **可交互控件**边界(输入框、复选框、下拉) |
| `--color-focus` | `--c-green-600` | 焦点环 |
| `--color-focus-inverse` | `--c-white` | 深色底上的焦点环 |
| `--color-warn` | `--c-amber-700` `#8a6d1a` | 缺口/注意 |
| `--color-danger` | `--c-red-700` `#a5472e` | 冲突/错误 |
| `--color-scrim` | `rgba(30,35,32,.32)` | 覆盖层遮罩 |

**`--color-border` 与 `--color-border-strong` 的分工是硬规则**,不是风格偏好:

| 元素 | 用哪个 | 理由 |
|---|---|---|
| 卡片 `.panel` 描边、列表分隔线、区块分割 | `--color-border` | 装饰性,无对比度要求 |
| `input` / `textarea` / `select` / 复选框 / 单选 / 可聚焦描边按钮 | `--color-border-strong` | WCAG 1.4.11 要求非文本 UI 边界 ≥3:1 |

### 1.2 字号

| 令牌 | 值 | 用途 |
|---|---|---|
| `--text-2xs` | 11px | **仅限**数字角标、纯拉丁标签 |
| `--text-xs` | 12px | caption、工具栏控件、表格次要列 |
| `--text-sm` | 13px | 列表次要信息、辅助说明 |
| `--text-base` | 14px | 界面正文(默认) |
| `--text-md` | 15px | 编辑器正文 |
| `--text-lg` | 17px | 区块标题 h3 |
| `--text-xl` | 23px | h2 |
| `--text-2xl` | 30px | h1 |

**中文最小字号 = 12px。** `--text-2xs`(11px)禁止用于中文。CJK 字形笔画密度显著高于拉丁字母,同字号下有效笔画间距更小,11px 中文的可读性明显低于 11px 英文。v1 未区分这一点。

### 1.3 行高与字重

| 令牌 | 值 | 用途 |
|---|---|---|
| `--leading-tight` | 1.4 | 标题、单行控件 |
| `--leading-normal` | 1.6 | 界面正文 |
| `--leading-relaxed` | 1.9 | 编辑器长文 |
| `--fw-normal` | 400 | 正文、工具栏控件 |
| `--fw-medium` | 500 | 标签、tab、按钮 |
| `--fw-semibold` | 600 | 标题 |

> v1 写的是「行高 1.6–1.9」。区间不是令牌——实现时会随机取值。

### 1.4 间距、圆角、尺寸

| 令牌 | 值 |
|---|---|
| `--space-1` … `--space-6` | 4 / 8 / 12 / 16 / 24 / 32px |
| `--radius-sm` | 6px(控件) |
| `--radius` | 9px(卡片) |
| `--radius-lg` | 14px(对话框、抽屉) |
| `--radius-pill` | 100px(胶囊标签) |
| `--control-h-sm` | 28px(工具栏紧凑控件) |
| `--control-h-md` | 36px(常规按钮、输入框) |
| `--control-h-lg` | 42px(设置页表单输入) |
| `--control-check` | 16px(复选框、单选) |

### 1.5 层级与阴影

| 令牌 | 值 | 用途 |
|---|---|---|
| `--shadow-sm` | `0 1px 2px rgba(30,35,32,.06)` | 卡片 |
| `--shadow-md` | `0 4px 12px rgba(30,35,32,.10)` | popover、菜单 |
| `--shadow-lg` | `0 12px 32px rgba(30,35,32,.16)` | 对话框、抽屉 |
| `--z-sticky` | 100 | 顶栏、tab 头 |
| `--z-dropdown` | 200 | popover、菜单 |
| `--z-drawer-scrim` | 300 | 抽屉遮罩 |
| `--z-drawer` | 310 | 窄屏抽屉、覆盖式右栏 |
| `--z-dialog-scrim` | 400 | 对话框遮罩 |
| `--z-dialog` | 410 | 对话框 |
| `--z-toast` | 500 | 全局提示 |

**规则**:CSS 中不得出现字面量 `z-index`。新增层级必须先加令牌。

### 1.6 布局尺寸

| 令牌 | 值 | 用途 |
|---|---|---|
| `--sidebar-width` | 218px | 左导航 |
| `--rail-width` | 340px | 右栏 |
| `--chat-column` | 728px | 聊天/正文列宽 |
| `--content-max` | 1080px | 主区内容上限 |
| `--page-max` | 1680px | 超宽屏整页上限(居中) |

`--content-max` 与 `--page-max` 是 v2 新增。v1 只有 `--chat-column: 728px`,在 2000px 宽屏下主区两侧留出大片空白,且整页无上限。

### 1.7 深色模式

**v1 范围内不实现。**

但语义层已经为它预留:未来只需在 `[data-theme="dark"]` 下重定义**语义层**,原始层与所有组件规则不动。这是分两层的第二个理由。

**因此**:任何 CSS 中出现的裸 hex 都会在那一天成为债。见 §9 令牌纪律。

---

## 2. 可访问性下限

以下是必须满足的,不是建议。

### 2.1 对比度

| 检查项 | 实测 | 要求 | 状态 |
|---|---|---|---|
| `--color-text` on `--color-bg` | 15.3:1 | ≥4.5 | ✓ |
| `--color-text-muted` on `--color-bg` | **4.81:1** | ≥4.5 | ✓ 勉强通过,不得再调浅 |
| `--color-border` on `--color-surface` | **1.34:1** | 装饰无要求 | ✓ 仅限装饰 |
| `--color-border-strong` on `--color-surface` | **3.22:1** | ≥3.0 | ✓ |
| `--color-primary` on `--color-surface` | 6.91:1 | ≥4.5 | ✓ |
| `#fff` on `--color-primary` | 6.91:1 | ≥4.5 | ✓ |
| `--color-warn` on `--color-surface` | 5.2:1 | ≥4.5 | ✓ |
| `--color-danger` on `--color-surface` | 5.6:1 | ≥4.5 | ✓ |

`--color-text-muted` 只有 4.81:1,**禁止**与 `--text-2xs`(11px)组合,与 `--text-xs`(12px)组合时仅用于非关键信息。

### 2.2 焦点可见

**每一个可交互元素必须有 `:focus-visible` 样式。** 无框架兜底,这是原生实现必须显式做的事。

```css
:where(a, button, input, select, textarea, [tabindex]):focus-visible {
  outline: 2px solid var(--color-focus);
  outline-offset: 2px;
}
/* 深色底(主按钮等)换用反色环 */
.primary:focus-visible { outline-color: var(--color-focus-inverse); }
```

禁止 `outline: none` 且不提供替代焦点样式。

### 2.3 命中区域

可点击目标 ≥28×28px。`--control-h-sm` 的控件若视觉高度不足,用透明 padding 补齐命中区。

---

## 3. 布局骨架

### 3.1 报告工作区(三区)

```
┌ 左导航 218px ┬──────── 主区 ────────┬ 右栏 340px ┐
│ 对象导航      │ 顶栏 / 内容 / 编辑器 │ tab 面板   │
└──────────────┴──────────────────────┴───────────┘
```

- **左导航** `.session-sidebar`:对象态入口 + 最近项,宽 `--sidebar-width`
- **主区**:顶栏 + 内容 + 编辑器;正文列宽 `--chat-column`(聊天)/ 自适应上限 `--content-max`(报告)
- **右栏** `.rail`:`BriefLoop` / `来源与数据` / `检查结果` 三 tab

### 3.2 首页

v1 未定义首页布局(只在零散小节里提到局部)。以下为 v2 补全。

**首页有两种形态,由数据量决定,不是两套布局:**

| 形态 | 条件 | 结构 |
|---|---|---|
| **空态** | 无运行中任务 且 无报告 且 无定时计划 | 单列居中,宽 `--chat-column`。右栏**不渲染**,底部区块**不渲染** |
| **常态** | 任一有内容 | 三区。右栏 `--rail-width` 常显,只渲染**有内容**的区块 |

**设计基准是常态,空态是降级。** 反过来会导致:为了填满空首页而加插图、加引导按钮、加折叠条,等用户有 20 份报告后这些全是冗余。

空态的内容顺序:问候语 → composer → 「你可以试试」四张卡。**没有额外的引导按钮**——composer 本身就是入口(见 §7.2)。

### 3.3 响应式

断点(CSS 自定义属性无法用于 media query,此处为文档常量):

| 断点 | 行为 |
|---|---|
| `>1600px` | 整页宽度上限 `--page-max`,居中 |
| `≤1150px` | 右栏改为覆盖式面板(`--z-drawer`);导航保留图标+文字 |
| `≤780px` | 导航收成窄条;右栏改抽屉式覆盖 |
| `≤560px` | 导航只留图标 |

---

## 4. 组件

### 4.1 清单

| 组件 | class | 说明 |
|---|---|---|
| 顶栏 | `.report-topbar` | 面包屑 + 标题 + 状态 chips + 右侧动作。高 64px,底部 `--color-border` |
| 状态 chip | `.chip` | 见 §4.2 |
| 分段 tab | `.seg-tabs` / `.rail-tabs` | 选中:`--color-text` + 底部 2px `--color-primary`;未选 `--color-text-muted` |
| 右栏 | `.rail` | tab 头 + 可滚动 pane,`[data-pane]` 切换,同时只显示一个 |
| 菜单 | `.menu-wrap > .popover` | 点击展开,点外部/Esc 关闭,`role=menu`,`--shadow-md` |
| 卡片 | `.panel` | `--color-surface` + `--color-border` + `--radius` + `--shadow-sm` |
| 按钮 | `.primary` / `.ghost` / `.outline` / `.wide` | 见 §4.3 |
| 来源行 | `.source-row` | 名称 + 状态徽标 + 定位入口 |
| 检查摘要 | `.check-summary` | 百分比 + 冲突/缺口数 |
| 发现 | `.finding` | 折叠项,severity 红/灰点;含 report_quote / requirement / evidence / suggestion |

### 4.2 状态 chip

| 状态 | 色 |
|---|---|
| 已保存 / 评分中 | 中性 |
| 已评分 | `--color-primary` |
| 有缺口 | `--color-warn` |
| 有冲突 | `--color-danger` |

**仅当状态存在才渲染。** 不渲染「无状态」的 chip。

### 4.3 按钮

| 变体 | 底 | 字 | 边 |
|---|---|---|---|
| `.primary` | `--color-primary` | `#fff` | 无 |
| `.outline` | `--color-surface` | `--color-text` | `--color-border-strong` |
| `.ghost` | 透明 | `--color-text` | 无,悬停 `--color-surface-soft` |
| `.wide` | 修饰符,占满宽度 | | |

### 4.4 状态矩阵(所有交互组件必须定义)

| | default | hover | active | focus-visible | disabled | loading |
|---|---|---|---|---|---|---|
| `.primary` | ✓ | ✓ | ✓ | ✓ 反色环 | ✓ | ✓ |
| `.outline` `.ghost` | ✓ | ✓ | ✓ | ✓ | ✓ | — |
| `input` `textarea` `select` | ✓ | ✓ | — | ✓ | ✓ | — |
| 复选框 / 单选 | ✓ | ✓ | — | ✓ | ✓ | — |
| tab | ✓ | ✓ | selected | ✓ | ✓ | — |
| `.source-row` | ✓ | ✓ | — | ✓ | — | ✓ |

`disabled`:`opacity: .45` + `cursor: not-allowed`,**保留可读对比度**,不得靠降透明度把文字压到 4.5:1 以下。
`loading`:保留原尺寸,不得因换成 spinner 导致布局跳动。

---

## 5. 分类色标(唯一允许多色的区域)

界面默认单色(绿/墨/灰)。**只有「内容分类语义」可以引入多色**;一旦某处用了多色,必须全站同一套,禁止只在首页当装饰。

### 5.1 色板

| 语义类 | tile 底 | 前景/描边 | 前景:tile | 用途 |
|---|---|---|---|---|
| 商业 / 周报 / 结构化分析 | `#E8F1FD` | `#1565C0` | 5.04:1 | 行业周报、公司进展、年报 |
| 证券 / 对比 | `#FCE9F1` | `#AD1457` | 5.99:1 | 竞品对比、券商研报 |
| 学术 / 资料 / 依据向 | `#E0F0EE` | `#00695C` | 5.62:1 | 已有资料成稿、学术路径 |
| 协作 / 会议 / 多方输入 | `#EFEAFD` | `#6741D9` | 5.35:1 | 会议纪要、多角色材料 |
| 中性兜底 | `#EEF0EE` | `#5B6360` | 5.40:1 | 未分类、通用报告 |

**v2 改了两个颜色,原因是色相冲突(v1 的实际缺陷):**

| 原值 | 问题 | 新值 |
|---|---|---|
| 学术 `#006838` | **与 `--color-primary` 完全同值**。分类图标看起来像主操作/成功态 | `#00695C`(青) |
| 证券 `#C62828` | 与 `--color-danger` `#a5472e` 同色相。红色图标 tile 与红色错误 chip 同屏时无法区分语义 | `#AD1457`(品红) |

**推导出的通则:分类色板不得占用绿与红色相**——这两个色相已被「主操作/成功」和「错误/冲突」独占。黄色相同理(`--color-warn`)。分类可用范围:蓝、青、紫、品红、中性。

### 5.2 落点

1. **首页建议卡** `.home-suggestion-icon`:按意图着色(周报→商业蓝,公司→商业蓝或协作紫,竞品→证券品红,资料→学术青),配可区分的**线性图标**,不是同色方块
2. **我的报告 / 最近的报告** `.report-card-icon`:按该报告绑定的内容方法或模板 genre 取同一 tile/前景色
3. **模板页 `GENRE_META`**(`frontend/app.js`):色板源头。改色只改这里和 `tokens.css`,两处同步

### 5.3 规则

- 分类色只出现在:图标 tile、列表左侧色条、筛选色点。**不铺大面积背景,不改正文色**
- 同一报告在首页、列表、导出预览中类别色必须一致
- 未写入 genre/内容方法的报告用中性兜底,**禁止随机取色**
- **若四色不承载分类语义(纯装饰),则禁止多色**——收成单色绿

---

## 6. 跨页规则

1. **顶栏只放对象级动作。** 行内/上下文动作放编辑器浮层或右栏 pane。
2. **同一动作只出现一处。** 包括入口按钮。例:「新建报告」只在左导航;评分只在右栏。
   *v1 违规实例:首页同时存在左导航「新建报告」、composer 发送、独立「生成第一份报告」按钮,三个绿色主按钮指向同一动作;「最近的报告」同时出现在右栏和底部折叠条。*
3. **空态不占位。** 无内容的区域**不渲染**——不放占位按钮,不放空状态插图,不放计数为 0 的折叠条。主区宽度随之回收。
   *(v1 §4.6 只约束了按钮,v2 泛化到区域。)*
4. **右栏**有内容时常显、可折叠,折叠状态按工作区记住;全部 pane 无内容时不渲染。
5. **渐进披露。** 用户表达意图**之前**不暴露执行参数。默认只留完成任务的最小控件集,其余进抽屉/popover。
6. **焦点可见**(§2.2)对所有可交互元素生效,无例外。
7. **文案不把内部约束表述成用户费用承诺。** 具体口径见 `DECISIONS.md`。
8. **测试钩子用 `data-testid`**,不用 `id` 或 class。见 §9.2。

---

## 7. 页面映射

### 7.1 报告工作区

- **顶栏**:面包屑 `#report-title` · 状态 chips `#report-status` · `版本`(`#version-select` + `#version-history`)· `导出`(Markdown / Word / 图文包 / 正式交付 / 审计包)· `⋯`(导入 Word 修订 / 导入 Markdown / 按审阅修改 / 独立审阅)
- **顶部 tab**:`编辑`(默认)· `数据与来源` · `检查与评价`;后两者直接切右栏对应 pane
- **右栏 tab**:`BriefLoop` · `来源与数据` · `检查结果`
- **编辑器**:格式工具条保留;`当前段落依据` 进检查 pane(后续版本改行内浮层)

### 7.2 Composer(首页 / 对话页共用)

**v2 重写。v1 的 §7「聊天工具栏」与「报告快捷选项」两节共列出 10 个常驻控件(权限、已有来源、宿主、模型、推理档位、发送方式、研究、事实核查、WikiSkill 轮数、更多),分两行常驻展示,违反 §6.5。**

**默认层(常驻,一行):**

| 控件 | 形态 |
|---|---|
| 附件 | 图标 + `+ 附件` |
| 模型 | 下拉,显示当前模型名 |
| 参数 | 图标按钮,打开 popover |
| 发送 | split button:主键 `发送` + 下拉(排队发送 / 定时) |

高度 `--control-h-sm`,`--text-xs`,`--fw-normal`,`--leading-normal`,`--color-text-muted`,垂直居中。

**参数层(`参数` popover 内):**

权限 · 已有来源 · 宿主 · 推理档位 · 研究档位 · 事实核查 · WikiSkill 及轮数 · 执行上限 · Scout 并发

**规则:**
- 附件图标与发送箭头保留固有图标尺寸
- 停止按钮保留 `--color-danger`
- 禁用控件保留禁用态(§4.4)
- 窄屏允许换行;**不单独缩小某一个控件的文字**
- 独立报告并发放设置页,不进 popover
- 排队等待与实际执行分开显示;无执行记录不推算执行用时

### 7.3 首页区块

按 §3.2,以下区块**仅在有内容时渲染**:

| 区块 | 位置 | 空态 |
|---|---|---|
| 运行中的任务 | 右栏 | 不渲染 |
| 最近的报告 | 右栏 | 不渲染 |
| 定时报告 | 主区,置于最近报告前 | 不渲染 |

「最近的报告」**只在右栏出现一次**(§6.2)。

定时报告用 `home-block` + `.panel`,名称、频率/时区、下一次执行、最近状态分行;操作共用现有按钮,窄屏自动换行;表单用原生 `dialog` + `label`,预设/自定义频率不展示 JSON。

### 7.4 设置 — 搜索策略

- 首选与补充策略并列两列,窄屏单列,字段间距 `--space-4`
- 补充渠道用紧凑 `.check` 行,复选框 `--control-check`,覆盖设置页 `--control-h-lg` 的最小高度;主色 `--color-primary`
- 分组用 `--color-border` + `--radius` + `--space-4` 内边距,不保留浏览器默认 `fieldset` 立体边框
- 显式保存;密钥独立简洁配置区 + 次级按钮,不回显
- 未接通的平台不展示选择入口(§6.3)
- WebUI 与 Electron 共用控件

### 7.5 运行时卡片

- 名称与本地品牌图标在首行;简介、检测版本、模型信息分行
- **品牌图标保留原色**(分类色规则的唯一豁免),其余继续用令牌
- 路径、协议、能力与诊断折叠展示
- 首页只保留可操作的宿主选择器,不重复显示「当前」宿主标签
- 窄屏操作按钮移至第二行,长版本号与模型 ID 自动换行

### 7.6 稿件改动视图

- 版本旁用紧凑「查看改动」入口,默认与上一稿对比,可选第一稿或中间稿
- 只读富文本双列保留表格、图片及格式
- 新增用 `--color-primary` 下划线,删除用 `--color-danger` 删除线,图表/格式变化用对应边框
- 标记**不写入**正文、保存版本或 Word 导出

---

## 8. 图标

- 线性图标,`stroke-width: 1.5`,`currentColor`
- 尺寸:16px(行内/控件)· 20px(导航/卡片)· 24px(区块标题)
- **同一语义全站同一图标。** 新增图标前先在现有集合里找
- 图标不单独承载信息:任何仅靠图标区分的选项必须有 `aria-label` 或可见文字

---

## 9. 纪律(让规则可执行)

规则不被机器检查就会失效——v1 已经写了「不得各写一套 hex」,但首页仍有独立色值。

### 9.1 令牌纪律

CSS 中**禁止裸 hex 与裸像素**,仅 `tokens.css` 豁免。stylelint:

```json
{
  "rules": {
    "declaration-property-value-disallowed-list": {
      "/^(color|background|background-color|border|border-color|box-shadow|outline-color)$/":
        ["/#[0-9a-fA-F]{3,8}/", "/\\brgba?\\(/"],
      "/^(z-index)$/": ["/^\\d+$/"]
    }
  },
  "overrides": [{ "files": ["**/tokens.css"], "rules": { "declaration-property-value-disallowed-list": null } }]
}
```

**本文档同样受此约束。** 规范正文引用令牌名(`--space-4`),不写裸数值(`16px`)。文档里出现裸 px,等于默许代码里出现裸 px——这是 v1 的实际情况。

### 9.2 测试钩子

v1 规则「保留既有 `#id`(前端测试依赖),结构重构用 class,不轻易改 id」是**把技术债写成了规范**:它用文档冻结 DOM,让每次重构都要先跟测试谈判。

**v2 方向**:测试挂 `data-testid`,`id` 回归语义/锚点用途。

**迁移期**(未完成前):`§7.1` 中列出的 `#id` 为白名单,可继续依赖;白名单外新增测试一律用 `data-testid`,**不得新增 id 依赖**。迁移完成后删除本节与白名单。

### 9.3 文档纪律

- 新增规则前先判断:换个功能是否仍成立?否 → 进 `DECISIONS.md`
- 每条规则应能回答:事实来源在哪?违反的后果是什么?兜底是什么?
- 改动记入 §11

---

## 10. 待决事项

| # | 事项 | 影响 | 建议 |
|---|---|---|---|
| 1 | `GENRE_META` 需按 §5.1 更新两个色值 | 模板页、首页卡、报告列表 | 与 `tokens.css` 同批改 |
| 2 | `data-testid` 迁移未启动 | 阻塞结构重构 | 按 §9.2 分批 |
| 3 | 深色模式未排期 | 语义层已预留,但裸 hex 会成为债 | 先落地 §9.1 的 stylelint |
| 4 | `--text-2xs`(11px)现有使用点未审计 | 中文可读性 | 全量搜索,中文场景升至 `--text-xs` |
| 5 | 图标集合未统一登记 | §8 无法校验 | 建立图标清单 |

---

## 11. 变更记录

### v2 — 2026-09-18

**结构**
- 拆出 `DECISIONS.md`:产品行为规格(删除语义、模型继承、宿主检测、额度口径)移出本文件
- 补 §0「本文档包含什么/不包含什么」,防止再次混入功能规格
- 全文重新编号(v1 从第 5 节起编号断裂,后四节为无编号 H2)

**令牌**
- 拆为原始层 / 语义层两级(v1 单层导致 `--green` 与学术分类色、`--line` 与输入框边界两处职责冲突)
- 新增 `--color-border-strong`(#8b918b,3.22:1),修复输入框/复选框边界 1.34:1 不满足 WCAG 1.4.11
- 新增焦点环、遮罩、z-index 刻度(6 级)、elevation(3 级)、控件高度刻度(3 级)、行高(3 级)、字重(3 级)
- 字号 8 个值全部命名(v1 有 8 值 6 名)
- 新增 `--content-max` / `--page-max`,修复超宽屏无上限
- 明确 v1 不做深色模式,并说明语义层如何为其预留

**可访问性**
- 新增 §2,含全量对比度实测值
- 新增 §2.2 焦点可见(v1 全文未提)
- 新增 §4.4 组件状态矩阵

**分类色标**
- 学术 `#006838` → `#00695C`(原值与 `--color-primary` 完全同值)
- 证券 `#C62828` → `#AD1457`(原值与 `--color-danger` 同色相)
- 新增通则:分类色板不得占用绿/红/黄色相

**规则**
- §6.3 空态不占位:从「按钮」泛化到「区域」
- §6.5 渐进披露:新增
- §9.2 测试钩子:`#id` 冻结规则 → `data-testid` 迁移路径

**页面**
- §3.2 补全首页布局(v1 缺失),定义空态/常态两形态及降级方向
- §7.2 重写 composer:10 个常驻控件 → 4 个默认 + 参数 popover(v1 §7 与「报告快捷选项」两节已被实现推翻)

**纪律**
- §9.1 stylelint 强制令牌,规则从约定变为保证

### v1
初版。
