# BriefLoop 界面设计系统（DESIGN.md）

范围：`src/briefloop/static/`（原生 HTML/CSS/JS，无构建框架）。本文件是界面重构的令牌与组件基准；改 UI 先改这里。

## 1. 布局骨架（三区）

桌面：

```
┌ 左导航 218px ┬──────── 主区 ────────┬ 右栏 340px（可折叠）┐
│ 对象导航      │ 顶栏 / 内容 / 编辑器 │ tab 面板            │
└──────────────┴──────────────────────┴────────────────────┘
```

- 左导航（`.session-sidebar`）：对象态入口 + 最近项；宽度 `--sidebar-width`。
- 主区：报告工作区为「顶栏 + 内容 + 编辑器」。正文列宽 `--chat-column`（聊天）/ 自适应（报告）。
- 右栏（`.rail`）：BriefLoop / 来源与数据 / 检查结果 三 tab；可折叠为 0。

响应式：
- `≤1150px`：右栏换成覆盖式面板；导航保留图标+文字。
- `≤780px`：导航收成窄条；右栏变抽屉式覆盖。
- `≤560px`：导航只留图标。

## 2. 令牌

| 令牌 | 值 | 用途 |
|---|---|---|
| `--green` | `#006838` | 主色、链接、主要按钮 |
| `--green-hover` | `#145238` | 主按钮悬停 |
| `--ink` | `#1e2320` | 正文 |
| `--muted` | `#6a706b` | 次要文字 |
| `--paper` | `#faf9f6` | 页面底色 |
| `--line` | `#dedfd8` | 分隔线/边框 |
| `--surface` | `#ffffff` | 卡片 |
| `--soft` | `#f4f6ef` | 次要面板底色 |
| `--warn` | `#8a6d1a` | 缺口/注意（黄） |
| `--danger` | `#a5472e` | 冲突/错误（红） |
| `--radius` | `9px` | 卡片圆角 |
| `--radius-sm` | `6px` | 控件圆角 |
| `--radius-pill` | `100px` | 胶囊标签 |
| `--space-1..6` | `4/8/12/16/24/32px` | 间距刻度 |
| `--shadow-sm` | `0 1px 2px rgba(30,35,32,.06)` | 卡片 |
| `--sidebar-width` | `218px` | 左导航 |
| `--rail-width` | `340px` | 右栏 |
| `--chat-column` | `728px` | 正文/聊天列宽 |

字号层级：`11/12/13/14/15/17/23/30px`（caption/label/body-sm/body/h2/h1）；行高 1.6–1.9。字体沿用 `--font`，等宽 `--mono`。

## 3. 组件

- **顶栏 `.report-topbar`**：面包屑 + 标题 + 状态 chips + 右侧动作（版本、导出、⋯）。高度 ~64px，底部分隔线。
- **状态 chip `.chip`**：`已保存`(中性) / `评分中`(中性) / `已评分`(绿) / `有缺口`(黄) / `有冲突`(红)。仅当状态存在才显示。
- **分段 tab `.seg-tabs` / `.rail-tabs`**：文字按钮，选中 `--ink` + 底部 2px `--green`；未选 `--muted`。
- **右栏 `.rail`**：`tab 头 + 可滚动 pane`。pane 用 `[data-pane]` 切换，同一时刻只显示一个。折叠按钮在 tab 行右侧。
- **菜单 `.menu-wrap > .popover`**：点击展开，点外部/Esc 关闭；`role=menu`。
- **卡片 `.panel`**：白底 + `--line` 边 + `--radius`。
- **按钮**：`.primary`(绿底白字) / `.ghost`(无边框，悬停浅底) / `.outline`(白底描边) / `.wide`(占满)。
- **来源行 `.source-row`**：名称 + 状态徽标（已核验/待核验/读取失败/需视觉）+ 定位入口。
- **检查摘要 `.check-summary`**：一项百分比 + 冲突/缺口数。
- **发现 `.finding`**：折叠项，severity 用红/灰点；含 report_quote / requirement / evidence / suggestion。

## 4. 规则

1. 顶栏只放「对象级」动作；行内/上下文动作放编辑器浮层或右栏 pane。
2. 同一动作只出现一处；不要顶栏和右栏各放一个（评分只在右栏）。
3. 右栏默认常显，可折叠；折叠状态按工作区记住。
4. 任何"受控工具额度"文案不得写成宿主费用上限（见 plan v0.2 §7）。
5. 保留既有 `#id`（前端测试依赖），结构重构用 class，不轻易改 id。
6. 未实现的功能不放占位按钮（宁可少一个 tab，也不放空壳）。

## 5. 报告工作区映射

- 顶栏：面包屑(`#report-title`) · 状态 chips(`#report-status`) · `版本`(`#version-select`+`#version-history`) · `导出`(Markdown/Word/图文包/正式交付/审计包) · `⋯`(导入 Word 修订/导入 Markdown/按审阅修改/独立审阅)。
- 顶部 tab：`编辑`(默认) · `数据与来源` · `检查与评价`；`数据与来源/检查与评价` 直接切右栏对应 pane。
- 右栏 tab：`BriefLoop` · `来源与数据` · `检查结果`。
- 编辑器：格式工具条保留；`当前段落依据` 进检查 pane（后续版本改行内浮层）。

## 6. 运行时卡片

- 名称与本地品牌图标在首行；简介、检测版本、模型信息分别展示。品牌图标保留原色，界面继续采用现有令牌。
- 当前选择的模型来自当前工作区设置；其他宿主不沿用当前宿主的模型，也不将模型目录中的首项当作已配置模型。
- 路径、协议、能力与诊断折叠展示；检测到 CLI 不表示账号调用成功。
- 首页只保留可操作的宿主选择器，移除重复的“当前”宿主标签；已运行回合的宿主保留在执行记录中。
- 窄屏将操作按钮放到第二行，长版本和模型 ID 自动换行。

## 7. 聊天工具栏

- 权限、已有来源、宿主、模型、推理档位和发送方式共用 `--font`、12px、400 字重、1.6 行高和 `--muted`；控件高度 28px，垂直居中。
- 附件加号与发送箭头保留图标尺寸，停止按钮保留警示色，禁用控件保留禁用状态。
- 首页与对话页共用样式，窄屏允许换行，不单独缩小某一个控件的文字。

## 8. 首页定时报告

- 置于最近报告前方，使用 home-block 与 panel，名称、频率/时区、下一次执行及最近状态分行。
- 操作共用现有按钮，窄屏自动换行；表单复用原生 dialog 和 label，预设/自定义频率不展示 JSON。
- 明示仅程序运行期间执行、不补跑离线时点；已有报告不受暂停/删除影响。

## 搜索策略设置

- 首选和补充策略并列为两列表单，窄屏自动单列；字段间距 16px。
- 允许的补充渠道采用紧凑 `.check` 行，复选框固定 16px，覆盖设置页普通 input 的 42px 最小高度；主色用 `--green`。
- 分组使用 `--line`、`--radius` 和 16px 内边距，不保留浏览器默认 fieldset 立体边框。
- 搜索策略显式保存；密钥独立的简洁配置区、次级按钮，不回显。
- 未接通的平台不展示选择入口。宿主原生用量未知单独呈现。WebUI 与 Electron 共用控件。

## 报告快捷选项
- 首页与新建报告共用一行小控件：研究、事实核查、WikiSkill及轮数、更多；12px字体、16px复选框，窄屏自然换行。
- 更多只放执行上限与Scout并发，独立报告并发放设置；不增加大卡片。
- 排队等待与实际执行分开显示，无执行记录不推算执行用时。

## 稿件改动

- 删除报告放在“更多操作”菜单，需二次确认；删除只把报告移出列表，保留来源、已下载文件及内部核查与学习引用记录。运行中任务未结束时拒绝删除。

- 版本旁使用紧凑“查看改动”入口，默认与上一稿对比，可选择第一稿或中间稿。
- 只读富文本双列保留表格、图片及格式；新增使用绿色下划线、删除使用红色删除线，图表/格式变化用对应边框。标记不写入正文、保存版本或 Word。

## 分类色标（唯一允许多色的区域）

界面默认单色（绿/墨/灰）。**只有「内容分类语义」可以引入多色**；一旦某处开始用多色，必须成为全站同一套色标，禁止只在首页当装饰色。

### 色板（与模板页 genre 一致）

| 语义类 | tile 底 | 前景/描边 | 用途示例 |
|---|---|---|---|
| 商业 / 周报 / 结构化分析 | `#E8F1FD` | `#1565C0` | 行业周报、公司进展、年报类 |
| 证券 / 对比 / 高亮冲突向 | `#FDECEA` | `#C62828` | 竞品对比、券商研报、需强对比的入口 |
| 学术 / 资料 / 依据向 | `#E6F2EC` | `#006838` | 已有资料成稿、学术路径 |
| 协作 / 会议 / 多方输入 | `#EFEAFD` | `#6741D9` | 会议纪要、多角色材料 |
| 中性兜底 | `#EEF0EE` | `#5B6360` | 未分类、通用报告 |

来源：模板页 `GENRE_META` 的 `tile`/`color`（`frontend/app.js`）。Home 四张「你可以试试」卡与「我的报告」列表 `.report-card-icon` 必须引用**同一映射**，不得各写一套 hex。

### 落点

1. **首页建议卡** `.home-suggestion-icon`：按意图着色（周报→商业蓝、公司→商业蓝或协作紫、竞品→证券珊瑚、资料→学术绿），并配可区分的线性图标；不再是四个同色方块。
2. **我的报告 / 最近的报告** `.report-card-icon`：按该报告绑定的内容方法或模板 genre 上同一 tile/前景色，列表与首页一眼可扫类别。
3. **模板页**：已是色板源头，改色只改这里与本表，两处同步。
4. **状态语义不混用**：绿=成功/主操作、黄=`--warn`、红=`--danger` 只表示状态，不表示分类；分类色不得复用做 chip 成功/失败。

### 规则

- 分类色只出现在图标 tile、列表左侧色条、筛选色点；不铺大面积背景，不改正文色。
- 同一报告在首页、列表、导出前的预览中类别色一致。
- 尚未写入 genre/内容方法的报告用中性兜底，禁止随机取色。
- 若四色不承载分类（纯装饰），则**禁止多色**——收成单色绿，回到令牌表。
